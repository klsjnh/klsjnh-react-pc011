# 2026-09-20 · 后端换版 11160 前端适配（原始对话记录）

> 011 协议 Step 1：原始对话入 `{YYYY-MM-DD}-chat.md`，禁写凭据 / 内网 IP / 密钥。
> 本文只记事实与结论，不复制正文；详细任务进度看同日的 `2026-09-20.md`。

## 一、用户输入（原始）

1. `http://192.168.3.160:11160/doc.html#/home` 读读后端文档
2. `D:\Source\java17\klsjnh-java17-framework011` 直接看后端代码吧
3. 低代码去新项目了 你先隐藏
4. 好的，按这个方案开始改造
5. （澄清）我在用我自己的模型，不涉及积分计费场景

## 二、输入解读

- 后端 11610 → 11160，且做了模块重组；前端需同步适配
- 低代码模块（lowcode011 / julyMetadata）已整体迁出至新项目，本仓库前端**只隐藏入口、保留页面代码**
- 消息中心（后端新增 33 端点）本期不接入，用户未置可否

## 三、已确认的后端重组事实（拉取实时 OpenAPI 核实）

| 变化 | 前端影响 |
|------|----------|
| user / role / userAudit 从 system011 迁出，独立为 iam 模块 | 前缀 `/klsjnh/system011` → `/klsjnh/iam`，需 baseOverride |
| datasource 只剩 julyDatasource | 前缀改名 `/klsjnh/datasource` |
| ai011 改名 aicenter | 前缀 `/klsjnh/ai011` → `/klsjnh/aicenter` |
| 新增 julyAiInference / julyAiImage / julyAiAudio | **本期不接入** |
| 新增 messagecenter（33 端点） | **本期不接入** |
| 低代码 / 业务建模后端代码全部移除 | 前端对应资源请求必然 404，仅隐藏入口 |

## 四、方案（用户已批准）

1. 迁移链路：代理 11160、dev 端口 11161
2. iam 三服务改走 IAM_BASE
3. datasource 前缀改名
4. aicenter 前缀改名
5. 隐藏低代码 + 业务建模菜单入口（页面代码保留）
6. 文档同步（013 / 015 / README）+ 落 OpenAPI 快照
7. 提交前跑 `./script011.sh gate`

## 五、结论

方案获批，进入逐项执行。执行进度见 `docs/2026-09-20.md`。
