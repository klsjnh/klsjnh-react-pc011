# 2026-09-24 — 后端换版对齐（11160 → 11130）

## 011.触发

用户指令：「192.168.3.160 11130 是新版，按这个对齐接口」。

## 013.盘点（只读，已落基线）

- **快照**：`docs/contracts/2026-09-24-openapi.json`（194 端点 / 240 schema；旧 09-20 = 168 / 216）。
- **差异**（详见 [requirement013/013.backend-realign-11130.md](../requirement013/013.backend-realign-11130.md)）：
  - 破坏性：`system011/julyMenu`·`julyOrganization` → **`iam`**（签名不变，纯前缀搬家）；`julyAiPrompt`（5）删除 → **`julyAiDomain`（17）** 取代。
  - 新增：`iam/julyPermCatalog`、`julyRole.assignObjectActions/getPermissionCodes`、`julyDictionary.import`、ai/storage/datasource 的 `getWithChildren`·`saveWhole`、`julyObject` 4 动作。
  - 未变：messagecenter 33 / config / scheduler / user / userAudit / datasource / sql / inference / demo11。

## 015.实施

1. **配置**：`vite.config.ts` dev 端口 `11161→11131`、proxy `11160→11130`；README / 015 / 013 / contracts README 同步。
2. **前缀对齐**：`services/system011/actions.ts` 注释更新；`julyMenuService`（5 处 fireApi + 2 处 get）与 `julyOrganizationService`（4 处）统一传 `IAM_BASE`。
3. **C 核验**：`julyAiPrompt` → `julyAiDomain` 前端**已于 2026-09-21 完成重构**，本轮静态核验 **15/15 端点与新契约逐字一致**（方法 GET/POST 亦对齐）。
4. **mock**：确认 mock 按 **action 串** 分发（不含 base），故前缀搬家**无需改 mock key**；menu / org / aiDomain 现有 mock 覆盖按既有设计（写操作 menu 走 `isMockMode` 短路、org 走 mock handler）。

## 017.门禁与自测

- `tsc --noEmit` **0**；`eslint src` **0**；规范自检 **ERROR 0 / WARN 1**（`types/view/mock.ts` 基线）。
- **静态自测**（脚本）：前端 action 串 vs 新契约逐条核对 —— `julyAiDomain` 15/15 对齐；全站未匹配项仅 `julyBusinessModeling` / `julyMetadata`（已迁新项目、入口隐藏，非本轮范围）。
- **⚠️ 未能跑浏览器联调**：本 WSL 环境 `node_modules` 装的是 **Windows 版 rollup**（`rollup-win32-x64-*`），Linux 下 dev server 无法启动（`MODULE_NOT_FOUND native binding`）。dev server 需在 Windows 侧启动后由人工复核 11131 ↔ 11130。

## 019.遗留

- **API 模式实联调**：待 Windows 侧起 dev（11131）→ 抽测 菜单管理 / 组织管理 / 提示词管理（julyAiDomain）。
- **新增能力未接入**（可选）：`julyPermCatalog` 对象权限、`getWithChildren`/`saveWhole`、`julyObject` 4 动作、`julyDictionary.import`。
- **环境**：`node_modules` 平台错配（Windows 安装、WSL 运行）——如需在 WSL 跑 dev，须在 Linux 侧重装依赖。
