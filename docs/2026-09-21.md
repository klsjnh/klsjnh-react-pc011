# 2026-09-21 工作日志

## 一、背景

用户要求：① 全局按钮风格统一（发现「新建用户」与「新建集团」不是一个风格）；② 以定时任务管理页为样本定 UI 标准并全局对齐；③ 在 docs 写 `031.pc-ui-standard.md`（英文）。另：上午按用户指示「先别管提示词了」，提示词相关线全部暂停。

## 二、任务清单

- [x] T1 环境启动：清杀旧 vite 进程（11161 被 PID 37504 + 残留 54608 占用，双杀）→ 分离式后台启动 dev server（PID 56184，vite 6.4.3，HMR 热模式，HTTP 200）
- [x] T2 全局按钮审计：`type="primary"` 实心按钮 31 处、`+` 文字前缀 8 处盘点归类
- [x] T3 全局对齐改造（12 文件）
- [x] T4 新建 `docs/031.pc-ui-standard.md`（中文，编号 031 为用户指定；初版英文同日即改中文）+ 索引同步（docs/README.md、011.agreements §015）
- [x] T5 归档历史日志至 `docs/archive011/`（09-12 / 09-17 / 09-20 / 09-20-chat）
- [x] T6 质量门禁：tsc + eslint + 规范自检
- [x] T7 修复 eslint 未使用导入 2 处（JulyAiPrompt 的 ReloadOutlined / OutboundMessagePane 的 PlusOutlined）

## 三、执行记录（T3 对齐明细）

**统一口径**（样本 julyScheduler + julyUser 既有约定注释提炼）：工具栏新建 = `color="primary" variant="filled"` + `PlusOutlined` + 文案「新建×」；危险/次要工具栏按钮 danger/default tonal；弹窗 footer 保存钮是全页唯一实心 `type="primary"`；行内操作 `type="link" size="small"`；禁止「+」文字前缀。

| 文件 | 改动 |
|------|------|
| `julyScheduler/index.tsx` | 新建任务：补 `PlusOutlined` 图标，去「+」前缀（原已 tonal） |
| `julyOrganization/index.tsx` | 新建集团：实心→tonal + 图标；行内「+ 子部门」→「子部门」 |
| `julyPermission/index.tsx` | 「+ 新建」→ tonal「新建角色」；「+ 添加用户」→ tonal + 图标 |
| `JulyDatasources/index.tsx` | 「+ 新建数据源」→ tonal「新建数据源」 |
| `business-pages.tsx` | 字典类型/字典项两个 extra 按钮 → tonal + 图标，去「+」 |
| `messageCenter/ChannelPane.tsx` | 新增通道 → tonal「新建通道」 |
| `messageCenter/TemplatePane.tsx` | 新增模板 → tonal「新建模板」 |
| `messageCenter/OutboundMessagePane.tsx` | 发送消息 → tonal（图标 SendOutlined，语义化） |
| `messageCenter/InboundMessagePane.tsx` | 模拟回调 → tonal |
| `julyMenu/index.tsx` | sider「新建顶级菜单」：link → tonal small + 图标 |
| `JulyAiPrompt/index.tsx` | 业务域「新建」→ tonal「新建域」（用户「先别管提示词」前已完成，仅按钮风格，无逻辑改动） |
| `StorageFormModal.tsx`（storageCenter） | footer「确定」→「保存」（弹窗确认钮文案统一） |

**事故与恢复（如实记录）**：T3 第一轮用 PowerShell 字符串批替换时，参数传递形态出错，把 `<` 批量替换成了 `B`，导致 `StorageFormModal.tsx` 与 4 个 messageCenter 文件 JSX 全局损坏。5 个文件在会话起点均为干净状态（git status 无 M 标记），全部 `git checkout --` 恢复原版后，改用 Node 精确补丁脚本（逐处先断言后替换，CRLF 自适应）重新落地。全仓 `BButton|BModal|BForm` 扫描 0 命中，无残留损伤。

**蓝本重写（第二轮，同日）**：用户指示「蓝本 用户管理 权限管理……搜索框 什么的 按钮 个数 状态 位置 单表 主子表 树表」。031 按两蓝本全文重写：§011 形态判定（单表 / 主子表左右分栏 / 树表 / 左树右表单，上下结构主子表定为存量保留不扩散）、§013 两蓝本页面骨架、§015 搜索区规格（Input.Search 宽 260 + Select 宽 140、搜索必回第一页、搜索框 1 + 筛选 ≤2 上限）、§016 按钮矩阵（单表工具栏固定排序与总数 ≤4、主子表按钮位置——保存为右区唯一实心、树表与左树右表单、状态语义与统一文案）、§017 三形态表格口径（勾选/列/Tag/固定列/实测高度/分页偏好、List+Empty 引导+Tabs 计数+草稿保存、树表无分页）、§020 基准实现索引；随后用户补「树主子表」形态（蓝本 = 菜单管理）。

**六型修正（用户指正「应该是五类 六类」）**：全页面枚举复盘后形态收敛为六型 —— ① 单表 ② 主子表·左右分栏（蓝本 julyPermission）③ 主子表·上下结构（蓝本 JulyAiModelProvider，同型字典管理/存储管理）④ 树表 ⑤ 树主子表 ⑥ 整页编辑器（蓝本 PromptDetailEditorPage）；辅助形态 = Tab 聚合页 / 仪表盘统计 / 特殊页。031 新增 §020 页面归类总表（全部页面逐页归类）与 §021 基准实现索引，§013/§015/§016/§017 补上下结构与整页编辑器的骨架、搜索、按钮、表格口径。

**顺带观察（不行动）**：后端 11160 当日 aicenter 端点重构 —— `julyAiPrompt` 8 端点变更为 `julyAiDomainPrompt`（getContent/render/getById/getByCode/insert/update/logicDelete/selectListByPage），`julyAiDomain` 7 端点上线（含 selectTree）；总路径 178 → 182。按用户指示提示词线暂停，未做任何对接。

## 四、质量自测

| 项 | 结果 |
|----|------|
| tsc --noEmit | **EXIT 0**，0 错误 |
| eslint | **EXIT 0**（修复 2 处未使用导入后） |
| 规范自检 | **ERROR 0 / WARN 1**（WARN 为历史遗留 `src/types/view/mock.ts` 的 any，与本次无关） |
| 审计复扫 | 全站 `type="primary"` 新建类按钮 0 残留；`+` 前缀 0 残留；tonal 按钮 67 处 |

## 五点五、字典管理对齐金标准 + 路由收敛（下午，用户多轮指令）

**背景与裁定**：用户确认金标准 = 用户管理 / 配置管理的单表结构（面包屑 → 标题 → 搜索+状态 → 新建/批量删除/备份/导出 → 表格）；字典管理（正版 `/system011/julyDictionary`，主子表上下结构）对齐该骨架。期间用户纠偏：**正版路由是 `/system011/julyDictionary`**（页面与路由都以它为准），`/business/dict` 是违约演示页。

**1. 路由收敛（一处一家）**：
- `routes.ts`：删除演示页 DictPage 懒加载与 `BUSINESS_PAGES.dict` 注册
- `constants.ts` GLOBAL_MENUS + `BusinessPage.tsx` 宫格卡：字典管理 → `SYSTEM011_ROUTES.julyDictionary`
- `business-pages.tsx`：整段删除演示字典页（原 L21-244）+ 孤儿导入（PlusOutlined / Empty / Modal / STATUS_OPTIONS）
- `types/view/dict.ts`：孤儿类型文件删除（仅演示段使用）+ barrel 行移除
- 后端菜单表核对：**无字典条目**（侧边栏由 GLOBAL_MENUS 兜底），无需后端动作

**2. 死循环修复（用户报 Maximum update depth exceeded）**：`DictionaryItemTable.tsx` 草稿重置 effect 依赖每次渲染重建的 `mappedItems` 数组引用 → setState → 重渲染 → 依赖再变 无限循环。修复：映射挪进 effect 内、依赖改为 store 里引用稳定的 `items`；全仓同类模式扫描零残留。

**3. 金标准对齐（主表工具栏）**：补两行结构 —— 第一行 `Input.Search`（宽 260，搜索编码/名称，onSearch 回第一页，query.keyword）+ 状态 Select（宽 140，全部/启用/停用，query.status）；按钮区维持 新建/导出（字典无批量删除后端能力，未配 rowSelection；导出已有）。

**4. 编辑弹窗重排（用户定稿 + 铁律）**：`DictionaryFormModal` 编码 / 名称 / 排序 / 状态 / 备注**各占一行**；状态必填（默认启用）。**逐字段核对线上 OpenAPI**：实体 `JulyDictionaryVo011` / `JulyDictionaryUpdateVo011`（required 仅 [id, dictionaryName]）/ `JulyDictionaryQueryVo011` 均有 status；**唯独 `JulyDictionaryInsertVo011` 无 status**（required [dictionaryCode, dictionaryName]）。

**5. Insert status 前端先行（用户裁定「你先做上 我让后端改」）**：`JulyDictionaryInsertVo011` 加 `status?`、`saveDictionary` insert 分支透传、mock insert 支持 status；后端补字段后零改动生效，留空后端默认启用。

**验证**：tsc EXIT 0 / eslint EXIT 0（孤儿导入清理后）/ 规范自检 ERROR 0 / WARN 1（历史遗留 mock.ts any）。

## 五点六、定时任务管理对齐配置管理金标准（下午，用户指令）

**用户指令**：「定时任务管理 对齐下配置管理」「有备注 你别管后面有没有你先加上」—— 备注 / 状态按前端先行套路加，后端补字段后零改动生效。

**线上契约核查（11160 实时）**：scheduler 端点 9 个（无 export / backup011 → 不加导出备份按钮）；`QueryVo` 无 status、`InsertVo` 无 status/remark、`UpdateVo` 无 remark、实体无 remark；`logicDeleteBatch` 存在（可做批量删除）。

**改造明细**：

| 层 | 文件 | 改动 |
|----|------|------|
| types | `julyScheduler/vo.ts` | 实体加 `remark?`；Query 加 `status?`；Insert 加 `status? / remark?`；Update 加 `remark?`（均注明前端先行） |
| types | `julyScheduler/view.ts` | `SaveSchedulerParams` 加 `remark?` |
| service | `julySchedulerService.ts` | saveScheduler 双分支透传 status/remark；**修真 bug**：`removeSchedulers` 原向 `logicDelete` 发裸数组（契约要求 IdVo011）→ 拆为 `removeScheduler(id)`（IdVo011）+ `removeSchedulerBatch(ids)`（logicDeleteBatch + IdsVo011），后者返回删除汇总 |
| mock | `julyScheduler.ts` | 分页筛选补 status；insert 落 status/remark；update 落 remark；`logicDelete` 改契约化单删（容忍历史裸数组）；补 `logicDeleteBatch` handler |
| page | `julyScheduler/index.tsx` | **重写**：两行工具栏（搜索 260 + 状态筛选 140 / 新建 + 批量删除）；表格加 rowSelection + 备注列；scroll 1200 × 实测高度；页头去副标题对齐配置管理 |
| page | `SchedulerFormModal.tsx`（新建抽离） | 弹窗逻辑从页面抽出独立文件；字段各占一行：编码（编辑锁定）/ 名称 / 处理器 / Cron / 状态必填（运行中/已停止，默认已停止）/ 备注 |

**验证**：tsc EXIT 0 / eslint EXIT 0 / 规范自检 ERROR 0 / WARN 1（历史遗留）。

**后端待改清单（scheduler）**：① `JulySchedulerQueryVo011` 加 `status`；② `JulySchedulerInsertVo011` 加 `status` / `remark`；③ `JulySchedulerUpdateVo011` 加 `remark`；④ 实体 / `JulySchedulerVo011` 加 `remark`。

## 五点七、用户管理手改复位对齐（下午，用户指令）

**背景**：用户手改了用户管理页（julyUser）的按钮：① 行内 编辑/删除 从 link 改为 filled 小按钮；② 工具栏配色试验（新建 green / 备份 blue / 导出 pink）且新建文案去掉「用户」后缀。随后指令「对齐下 配置管理」。

**复位对齐（配置管理金标准口径）**：
- 工具栏配色回归主题 token：green → `primary`、blue/pink → `default`（配色语义由主题令牌表达，不引入散点色）
- 新建文案回「新建用户」（页面级新建 = 新建×，与配置管理「新建配置」同构）
- 行内操作回 `type="link" size="small"`（删除 link + danger + Popconfirm）—— 行内不用 filled 按钮

**验证**：tsc EXIT 0 / eslint EXIT 0 / 规范自检 ERROR 0 / WARN 1（历史遗留）。

## 五点八、全站备注/状态铁律 + 单表开发手册（傍晚，用户指令）

**全站铁律落地（用户指令「所有的页面检查下 确定备注和状态 备注上 状态下 都要独立成行 备注要多行」）**：16 处表单对齐 ——
- 换序（备注移到状态前）：ProviderFormModal / ApiFormModal（顺带把排序从 Row/Col 并排拆出独占一行）/ DomainFormModal / PromptFormModal / RoleFormModal（描述） / SchedulerFormModal / DictionaryFormModal / StorageFormModal / PromptDetailEditorPage / MetadataEditorDrawer / MetadataFormPage / ModelingFormPage（备注整行前置、状态保持与关联数据源/对象类型同行组前）
- 单字段备注改多行：ChannelFormModal / TemplateFormModal（maxLength 255 保留）/ DatasourceFormModal
- ConfigFormModal 已合规（SKIP）
- 规则同步写进 [016 §9.1](016.coding-standards.md)：备注在状态前 / 各自独占一行 / 备注 TextArea 多行
- 验证：tsc EXIT 0 / eslint EXIT 0 / 规范自检 ERROR 0 / WARN 1（历史遗留）

**单表开发手册（用户指令）**：新建 `docs/dev-guides/011.single-table-dev-standard.md`（目录 / 文件名 / 标题英文、内容中文；初版曾按用户原话落在「开发手册(英文)/」，同日改英文名）——以配置管理为蓝本的单表开发流水线：动手前核查（契约先行 / 形态判定 / 能力盘点 / 前端先行套路）、3 文件拆分与职责边界、壳-表-弹窗三段规格、数据层五件套、提交自查清单、本轮三个实改页面作对照实例。索引已入 docs/README.md。
- **编号说明**：手册号 `011` 与 `011.agreements.md` 同号 —— 属用户指定（放专题目录内，不占全局序列），已如实登记；如需改号由用户裁定。
- 验证：docs 变更不触代码门禁，沿用最近一次全绿结果。

## 五、遗留

- 编号说明：`031` 为用户指定号（仓库内按序列下一个空闲号实为 021；跳号服从用户指令，已登记）
- 031 语言：用户先要求英文、随后明确改中文；再以「用户管理 / 权限管理」为蓝本重写至搜索框 / 按钮个数 / 状态 / 位置 / 三形态粒度（2026-09-21）
- 上下结构主子表（存储 / AI 供应商）为存量形态：只维护不新增，新主子表一律抄权限管理左右分栏
- 工作区含 09-20 遗留未提交改动（提示词翻转 §十五/十六等），与本轮改动并存，均未提交
- 后端 aicenter 新契约（julyAiDomainPrompt / julyAiDomain）待提示词线重启时对接
- **后端待改**：`JulyDictionaryInsertVo011` 补 `status` 字段（前端已先行透传，Update/Query/实体均已有 status；insert 留空默认启用）；~~scheduler status/remark~~（已在 0.0.50 后核对：待后端确认 Query.status / Insert.status+remark / Update.remark / 实体.remark 落地）
- 消息中心菜单 update 500 待后端恢复后复验；`testProviderConnection` 孤立代码待口径明确后清理
