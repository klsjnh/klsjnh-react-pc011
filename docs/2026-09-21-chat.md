# 2026-09-21 原始对话记录（要点）

> 011 协议 Step 1；禁写凭据 / 内网 IP / 密钥。详细进度见同日 `2026-09-21.md`。

1. 启动 11161，杀掉其他已启动的进程，热模式开发
2. （排查后端时发现 aicenter 端点重构：julyAiPrompt 8 端点改为 julyAiDomainPrompt，julyAiDomain 7 端点上线）「做的还是不对 先别管提示词了」→ 提示词线全部暂停
3. 「你看看 全局的按钮 新建用户 和 新建集团 不是一个 风格」→ 全局按钮审计
4. 「定时任务管理 我们规定一个 Ui 标准 编码 下面是按钮 你看看 铁律 另外看看全局 有不一样的对其一下」→ 以 julyScheduler 为样本定标准 + 全局对齐
5. 「你先这样 docs 写个031.pc ui 标准（英文）」→ 新建 `031.pc-ui-standard.md` + 索引同步
6. 「031.pc-ui-standard  文件内容用中文」→ 全文改中文落位
7. 「蓝本 用户管理 权限管理……搜索框 什么的 按钮 个数 状态 位置 单表 主子表 树表」→ 031 以两蓝本重写（搜索区规格 / 按钮个数·状态·位置 / 单表·主子表·树表三形态）；顺带修复 eslint 未使用导入 2 处，门禁全绿（tsc 0 / eslint 0 / 规范 0 错误）
8. 「单表 主子表 树表 树主子表」→ 形态收敛为四型：原「左树右表单」并入「树主子表」（蓝本 = 菜单管理）
9. 「很好 你先检查页面」「你仔细看看吧 应该是五类 六类 你分的不对」→ 全页面枚举复盘，形态修正为**六型 + 辅助形态**（主子表拆左右分栏/上下结构两型、补整页编辑器型；辅助形态 = Tab 聚合 / 仪表盘 / 特殊页），新增 §020 页面归类总表（全部 40+ 文件归类）
10. 「金标准 = 用户管理/配置管理 单表（面包屑/标题/搜索状态/新建批量删除备份导出/表格）」「字典管理对齐」（曾误发又撤回，后重新确认）→ 字典管理正版页对齐金标准
11. 「你先把主子表对齐单表」「路径和路由不一致 /system011/julyDictionary 是违约的」「操 /system011/julyDictionary 是正版的 路由和页面都是这个」→ 路由收敛：/system011/julyDictionary 唯一正版，/business/dict 演示页全链路清除
12. 报「Maximum update depth exceeded」→ 定位 DictionaryItemTable 草稿 effect 依赖每次渲染重建的数组 → 修复（依赖改稳定 items 引用）
13. 「字典管理不复合 编辑的时候 状态和备注 状态必填 状态一行 备注一行 编码 名称各一张 然后是排序」「你先做上 我让后端改」→ 编辑弹窗五行重排 + 状态必填；逐字段核对线上 OpenAPI 发现 InsertVo 无 status → 前端先行透传，后端待补
14. 「定时任务管理 对齐下配置管理」「有备注 你别管后面有没有你先加上」→ 定时任务页对齐金标准（两行工具栏 + 批量删除 + 备注列），弹窗抽独立 SchedulerFormModal（字段独行 + 状态必填 + 备注）；顺带修 removeSchedulers 契约 bug（裸数组 → IdVo011/logicDeleteBatch）；status/remark 前端先行，后端待补（Query.status / Insert.status+remark / Update.remark / 实体.remark）
16. 「julyDictionary 拆成3个组件 index 主表列表 主表编辑 子表列表」（4 文件）→ DictionaryTable 新抽出，index 瘦身为壳；门禁全绿
17. 「用户管理拆成 index 表格和弹窗 3个文件」→ UserTable 新抽出，JulyUserFormModal 复用，index 瘦身为壳；手改全部保留；门禁全绿
18. 「重新读取现在的 用户管理 设置为单表金标准 然后用这个金标准去修改 julyconfig」→ julyConfig 对齐：green 新建（文案「新建」）/ blue 备份 / pink 导出 / 行内 filled 按钮 / 状态筛选（Query.status 前后端均已支持）/ 状态列宽 90；类型与 mock 同步
19. 「配置管理 后端有了 remark」「编辑 配置值是多行 状态后面加备注 然后也是多行 文件也是 index 表格和弹窗」→ 线上核实 remark/status 全链路上线；ConfigFormModal 多行重排 + remark 接入；拆 3 文件（ConfigTable 新抽）
20. 「先备注后 状态 另外 编辑也是」→ 表格列序与弹窗字段序统一为备注在前
21. 「所有的页面检查下 确定备注和状态 备注上 状态下 都要独立成行 备注要多行」→ 全站 16 处对齐 + 016 §9.1 铁律更新；门禁全绿
22. 「docs/开发手册(英文)/011.单表开发标准（英文） 内容是中文」→ 新建单表开发手册（蓝本 = 配置管理，内容中文），README 索引同步
23. 「目录名字 文件名字 都应该是英文」→ 目录改 docs/dev-guides/、文件改 011.single-table-dev-standard.md（标题同步英文），内容保持中文
24. 「以字典为例 看看后台接口 先对齐 现在的字典样式（字典的文件分组是对的 现在ui不对」→ 重拉线上契约：字典 Insert/InsertItem 已带 status+remark（后端改单交付）；前端转正对齐（类型注释 / service 透传 / mock 落库 / 移除停用二次提交 workaround）；UI 对齐蓝本：主表 Card + 实测高度、明细补备注列（可编辑）、scroll 加宽；门禁全绿
25. 「主子表的情况下 新增和导出和搜索在一行」「分页 page 就是10 去掉选择」→ 字典 + AI 供应商主子表：单行工具栏、分页固定 10 去掉条数选择；单表金标准页暂不变
26. 「按钮靠左 靠着状态」→ 工具栏合并一行（首版被 page-toolbar 的 space-between 拉满整行，用户截图痛斥后修：包进 toolbar-left 紧排靠左）
27. 「都靠左」（指表格居中）→ 单元格与表头一律左对齐：字典主表/明细 + AI 供应商主子表删除 hdrCenter 居中写法；门禁全绿
28. 「字典管理我改过了 ui样式 按钮都不对」→ 用户已改明细操作条为 tonal small；AI 补齐工具栏（green 新建/pink 导出）与行内 filled 小按钮（主表 + 明细 全部动作）；门禁全绿
29. 「可以了 写文档吧 这算是主子表的金标准了」→ 字典管理定稿为主子表金标准；新建 dev-guides/013.master-sub-dev-standard.md（文件名/标题英文、内容中文），README 索引同步
30. 「你看看后台的 ai提示重新开发了吧」→ 实测 16:39 版契约（域树+独立提示词资源）并出评估底稿
31. 「等下吧 后端接口有问题 / 你先看看代码 把该拆的拆开吧」→ 拆分 JulyAiPrompt：338 行 → 壳/DomainPanel/PromptPanel/usePromptDetails（N+1 隔离层），行为零变化门禁全绿
32. 「后端提示词的业务接口好了 你看看」→ 复测契约反转：julyAiDomainPrompt 并回 julyAiDomain（17 端点 bundle，saveWhole/getWithChildren 新增）；021 评估按新契约重写落 docs，迁移底稿待拍板
33. 「批准 / 左侧和菜单管理一样是树 / 能不能写个组件 / KlsjnhTreeList011 / 右击菜单呢」→ 新组件 KlsjnhTreeList011（树面板+右键菜单）落 klsjnh011；提示线按 bundle 契约全量迁移（15 文件+删 N+1 隔离层，fetchPromptsByDomain 按域直查）；门禁全绿
34. 「全面检查下代码质量吧」→ 逐文件审查迁移产物，审出 5 处问题全部修复（service 刷新链断裂[关键]/防环写反[关键]/正文回填竞态/编辑页 new 死路径/拖拽悬空）；门禁复跑全绿
35. 「死代码呢」→ 全量导出符号零调用检测：清 9 个 service 死导出（分页机制整套退役、ACTIONS 收窄 4 端点）、domain store 简化为 {tree,loading}、删 2 个零引用类型；门禁复跑全绿
36. 「那就是违反铁律了？」→ 查证五层防线均无死代码条款（规则缺口非违规，检查清单漏类已认）；用户批准立规：016 §4.2 导出纪律 + §16.1 自查清单 + 规范自检脚本零引用导出扫描上线（首跑 WARN 45 条历史存量待人审）
37. 「处理所有错误代码 和不合规的代码和没引用的代码」→ 死文件 2 + 零引用符号 44 + 连环死代码 3 + 孤儿导入 10 全部清除；mock 边界 any 保留（豁免论证在案）；检查器 WARN 46→1，门禁全绿
15. 「用户管理我改了 尤其是按钮 和对齐下 配置管理」→ **执行走偏并已勘误**：误把用户手改（green 新建 / blue 备份 / pink 导出 / 行内 filled）当旧风格复位；用户纠正「是不是把我改的改回去了」，随后确认手改版即**单表金标准**（工作区现保存的就是手改版），julyUser 不再被改动
