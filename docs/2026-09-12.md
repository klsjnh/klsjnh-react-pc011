# 2026-09-12 工作日志（pc-admin 前端）

## 今日完成

### 移动端 → PC 端改造收尾
- 权限管理页重构：左角色列表（CRUD）+ 右页签（菜单权限树默认收起 · 右上角保存 / 关联用户穿梭框）
- 业务中心 PC 化：19 个子页面（表格类 → page-header + 工具栏 + data-table；面板类 → 多列指标卡/宽幅图表）+ 占位页
- 消息通知 / 帮助反馈 / 关于系统 / 个人中心 PC 化；清理死链接（/permissions/relation、/roles）

### 结构与命名
- 系统管理四页收敛至 `src/pages/system011/`，`julyXxx` 全单词命名（julyMenu / julyUser / julyPermission / julyOrganization）
- 路由同步为 `/menu` `/user` `/permission` `/organization`（侧边栏、个人中心、mock 菜单配置同步）

### 顶栏
- 通知铃铛 + 未读红点（新建 notificationStore，与消息通知页共用数据、实时联动）
- 用户下拉菜单（个人信息 / 修改密码弹窗 / 退出登录）
- 数据模式徽标（MOCK / API 切换 + API 地址配置 + 最近错误展示）

### Mock / API 双模式
- 新增 `src/config/appConfig.ts`（模式 + API 地址，localStorage 持久化，支持 VITE_DATA_MODE / VITE_API_BASE_URL）
- 新增 `src/api/request.ts`：POST + 统一响应信封（对齐 016 契约）、8s 超时、失败回退、fireApi 乐观更新
- menuStore / roleStore / notificationStore 双模式接入（读取 selectListByPage、写入标准动作）；各含 reload()
- `mock-server.mjs` 测试后端（信封格式，node mock-server.mjs [端口]）

### 持久化
- 新增 `src/stores/uiStore.ts`（localStorage）：侧边栏收起状态、菜单树展开/选中

### 文档收敛
- 新增详版 `README.md`（总览入口）+ `.env.example`
- docs 按后端文档体系补齐：`011.agreements.md`（编号/归档约定）、`README.md`（索引）、`013.project-info.md`（前后端模块对应）、`015.coding-standards.md`（前端编码标准）、`016.api-contract.md`（同步后端权威版 + 前端请求行为）
- 删除遗留 `src/utils/request.ts`（无引用、硬编码旧地址）

## 待办

- [ ] 用户管理页列表数据接入 store / API 体系（现为页面本地 mock）
- [ ] 死代码清理：`RoleFormPage.tsx`、`PermissionRelationPage.tsx`、`management-pages.tsx` 未使用导出
- [ ] 面包屑对业务子路由（/business/*）显示"仪表盘"的旧匹配逻辑待修
- [ ] 历史类型报错清理（roleStore 初始 users 缺 departmentId、julyPermission 两处）
