# 013 — 后端换版对齐（11160 → 11130）

> 关联：[017.tech-debt-redlines](../017.tech-debt-redlines.md) §C1（契约唯一真源）/ §C2（禁写死前缀）/ §E3（不跳层）· [013.api-contract](../013.api-contract.md)
> 类型：**触碰对外契约的变更**（模块搬家 + 端点重构）→ 按 §E3 补本方案层。

## 011.背景

- **2026-09-24 后端换版**：新版本运行在 `http://192.168.3.160:11130`（旧 `11160` 仍在，非目标）。
- 契约快照已落盘：`docs/contracts/2026-09-24-openapi.json`（194 端点 / 240 schema；旧 2026-09-20 = 168 / 216）。
- 差异源：逐端点比对 + 逐 schema 比对（本文仅记结论，事实以快照为准，不复制正文 —— §E2）。

## 013.变更清单

### 013.1 破坏性（不改会 404 / 500）

| # | 变更 | 处置 |
|---|------|------|
| 1 | `system011/julyMenu/v1/*`（10）→ `iam/julyMenu/v1/*`；**签名逐字不变，纯前缀搬家** | `menu.*` 调用统一传 `IAM_BASE` |
| 2 | `system011/julyOrganization/v1/*`（8）→ `iam/julyOrganization/v1/*`；**纯前缀搬家** | `organization.*` 调用统一传 `IAM_BASE` |
| 3 | `aicenter/julyAiPrompt/v1/*`（5）**删除** → `aicenter/julyAiDomain/v1/*`（17） | **前端已于 2026-09-21 按新契约重构完毕**（julyAiPromptService 实际打 julyAiDomain 明细端点）→ 本轮只做一致性核验 |
| 4 | `julyMenu.getTree/getUserMenuTree/getById`、`julyOrganization.getTree` 等方法为 **GET**（其余 POST） | 前端 `api.get` 已正确；本版 HTTP 方法**未变**（11160 同口径），无需改 |

### 013.2 新增能力（非破坏，本期可暂不接入）

`iam/julyRole.assignObjectActions` · `getPermissionCodes`；`iam/julyPermCatalog.selectObjects` · `selectActions`（**新对象权限模型**）；`julyDictionary.import`；`julyAiModelProvider.getWithChildren` · `saveWhole`；`julySync.getWithChildren` · `saveWhole`；`julyStorage.getWithChildren`；`julyObject.copyObject` · `renameObject` · `downloadObjectStream` · `selectObjectPage`。

### 013.3 未变

`messagecenter`（33）· `julyConfig` · `julyScheduler` · `julyUser` · `julyUserAudit` · `julyDatasource` · `julySql` · `julyAiInference` · `demo11` —— **全部原样**。字段级仅补 `description`，**字段名/类型未变**，前端类型无需改。

## 015.实施

1. **配置**：`vite.config.ts` dev 端口 `11161→11131`、proxy `11160→11130`；README / 015 / 013 同步。
2. **前缀对齐**：`services/system011/actions.ts` 的 `menu` / `organization` 组改传 `IAM_BASE`（与 user / role 同口径）；mock 路由键同步（mock 按真实 action 串分发）。
3. **核验 C**：确认 `julyAiPromptService` / `julyAiDomainService` 的 17 个 action 与新契约逐字一致（本方案已核，见 §013.1 #3）。
4. **门禁 + 自测**：`gate` 全绿；mock 模式全链路；API 模式（11130）抽测 menu / organization / config。

## 017.风险

- **mock 与真接口同源**：改 action 串必须同步 `mock/`，否则 mock 模式 404（本项目 mock 按 action 路径分发）。
- **`IAM_BASE` 语义**：仅改 `menu` / `organization` 两组；`config` / `scheduler` / `dictionary` 仍走默认 `apiBaseUrl`（`system011`），**不可误改**。
- **低代码 / 业务建模**：`julyMetadata` / `julyBusinessModeling` 已迁往新项目（旧契约仍在 11130），前端入口隐藏、代码保留，本轮**不动**。
