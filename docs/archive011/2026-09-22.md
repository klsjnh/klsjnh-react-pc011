# 2026-09-22 工作日志

## 1. 工作区垃圾清理 — 上午

- **.vf 保留**（内容"57"）：script011.sh L78 的版本计数器，勿删。
- 删 git 跟踪垃圾 8 件：all-paths.txt / extract-all.ps1 / extract-menu.mjs / temp-menu-paths.mjs / tmp_inspect.mjs / tmp_sw.mjs / tmp_new_openapi.json（153KB）/ storagecenter-openapi-full.json（1MB 冗余契约快照，09-18 已合并进 docs/contracts/2026-09-17-openapi.json，删前验证合并件在位）。
- 删未跟踪垃圾：.openclaw-*.exit/.log 4 件、package-lock.json（与 pnpm-lock.yaml 冲突）。
- .workbuddy 清 17 件临时产物（日志/UI 截图/过期快照），.mjs 复现脚本保留。
- .gitignore 增补 `.openclaw-*.exit`。
- 以上跟踪文件的删除随历史重写消失（见下节），不再占用下次提交。

## 2. git 历史清理（重写）— 上午

**触发**：工作区垃圾清完后用户要求「历史 git 也清理干净」。侦察发现 11 个垃圾 blob ~1.2MB 仍在历史提交链（pack 才 524KB），其中 `docs/swagger-api-docs.json`、`docs/swagger-live.json`、`storagecenter-openapi-full.json` 三个 blob **含内网地址 192.168.3.160**，远端为 GitHub → 定性为安全整改。

**执行**（用户授权 agent 操作 WSL dev016，filter-repo + GitHub SSH 仅该发行版齐备）：
1. `~/pc011-rewrite.git` 镜像克隆（不碰工作仓库）→ bundle 备份 `~/pc011-backup-pre-rewrite-20260922.bundle`（1.4MB）
2. `git filter-repo --force --prune-empty never --invert-paths --path <P>` 剔除 12 条路径（8 临时脚本/快照 + 2 swagger 快照 + 2 前轮已删的 extract/temp 脚本）。**语法要点：路径必须逐条 `--path` 前缀，裸排报 unrecognized arguments**
3. 三重核验：V1 垃圾路径零残留 PASS / V3 提交数 58 不变 PASS / V2 内网地址初报 FAIL —— 复查定性：三份污染快照已剔除（V1 佐证），剩余命中全部为**现存有意配置**（vite.config.ts 代理、README、docs 记录），在旧 main 上本就公开，非新增暴露面 → 放行
4. `git push --force URL main`（mirror 克隆要绕开 refspec 限制直连 URL）→ 远端 HEAD 校验一致：`c952361 → b797db4`
5. **GitHub 全新克隆终验**：垃圾路径不可达 / 3 个污染 blob id（ff0bbeea、b06bea61、b59db977）不存在 / 58 提交完好 / 工程文件完好 → FINAL-VERIFY-OK

**Windows 工作仓对齐**（Windows git 无 GitHub SSH，走 bundle 中转）：
- `git fetch <bundle> main` + `git reset --hard FETCH_HEAD`（重写后哈希全变，不能 pull）+ `git update-ref refs/remotes/origin/main` → HEAD = b797db4
- 旧历史清除链：`backup/2026-09-20-local-0048` 分支删除 → **stash（rescue-2026-09-20 routes-lowcode-hiding）挂在旧历史上阻断 gc**，先 `git stash show -p` 归档 patch 到仓库外（`D:\Source\react011\stash-rescue-20260920-archive.patch`）再 `git stash clear` → 清孤儿 pack `.rev` 文件 → `git reflog expire --expire=now --all` + `git gc --prune=now` → **5 个污染 blob 本机全部消失**，`--all` 计数 99→58，size-pack 1.43→1.31 MiB

**备份（确认无误后可删）**：`D:\Source\react011\klsjnh-react-pc011-backup-pre-rewrite-20260922.bundle`、WSL dev016 `~/pc011-backup-pre-rewrite-20260922.bundle`（重写副本与中转 bundle 已清）
**注意**：`vite.config.ts`、README、docs 里的内网地址为有意配置仍存在；若仓库需转 Private 或收紧泄露面，另行处理
**遗留**：devserver.log/.err.log 仍被 dev 进程锁定，重启 dev server 后删

## 3. 五色全站落地 + UI 原语（PageToolbar / StatusTag / SearchInput）— 上午

**触发**：用户点出三大问题 —— ① 五色未全站落地（scheduler/数据源/AI 供应商/消息中心新建仍 color="primary"）；② 有规范无原语（工具栏/搜索行大量 inline style + 复制粘贴）；③ 令牌漂移（.search-input 写 240 应为 260；toolbar-row-search 未进 SCSS）。

**执行**：
1. **三原语落位 klsjnh011**（第 5-7 成员）：`KlsjnhPageToolbar011`（stacked 两行=金标准 / inline 单行）+ `KlsjnhBatchDeleteButton011`（danger filled + Popconfirm + 双态）+ `KlsjnhStatusTag011`（'1'→启用 green / '0'→停用 red，labels/colors 覆写）+ `KlsjnhSearchInput011`（宽 260 类出，类型 = InputProps + Pick<SearchProps,'onSearch'>）；barrel 对齐 @/ 别名（相对路径被检查器 ERROR 拦过一次）。
2. **令牌修正**：`.search-input` 240→260（_common.scss + global.css 死文件同步）；`.toolbar-row-search` 正式定义进 _common.scss（flex/gap12/mb12，页面 inline style 退役）；storage 筛选 Select 240→140；lowcode 搜索 280→原语 260。
3. **五色落地**：批量替换 24 处 → 逐处复核回退 11 处误伤（子表新增/树根入口/分栏主项/行级新增一行按 031 §016 仍为 tonal primary）→ 净落 13 处 green：scheduler / 数据源 / 业务建模 / AI 供应商 / AI 提示 / 消息中心×4 / 存储×2 / lowcode 列表。
4. **原语迁移**：julyScheduler + julyConfig（stacked）/ JulyDatasources + AI 供应商（inline，320 漂移一并修）工具栏换 KlsjnhPageToolbar011；消息中心×4 + lowcode 列表批量删除换 KlsjnhBatchDeleteButton011（补齐缺失的 Popconfirm）；StatusTag 收敛 8 页状态列（scheduler 停用色保留 default 覆写）。
5. **金标准 julyUser 与 julyDictionary 四件套不碰**（031 明文"以 julyUser 为准"）。

**踩坑**：批量替换 color="primary"→green 必须带图标/动作词白名单并逐处复核语义边界（031 的 primary 有三类合法存在：保存场景、子表新增、行内编辑小钮）；脚本幂等守卫用"import 行内含组件名"判定，误用全文 includes 会被 render 代码骗过。

**门禁**：tsc 0 / eslint 0 / 检查器 ERROR 0 WARN 1（mock any 豁免）/ dev 200。**变更**：17 文件 +159/−143 + 新增 3 原语文件 + 031 四处同步（§015/016/017 + 修订日志）。


## 4. 树表金标准定稿 + 按钮 size 全站立规 — 下午

**触发**：用户定稿「/aiCenter/julyAiPrompt 可作为树表的金标准」，并要求全站盘点按钮 size。

**盘点结论（脚本实测 170 个 <Button>）**：small 87 / 默认 middle 83 / large 0 —— 即约半数按钮未写 size，但这**符合金标准口径**：julyUser/julyConfig/julyDictionary 的工具栏主按钮（新建/批量删除/备份/导出）实测全部默认 middle；弹窗 footer 全部走 `<Modal okText="保存">` 内建（无一处手写 footer Button）。

**size 口径立规（031 §016）**：工具栏主按钮 / 弹窗 footer / 整页编辑器页头 = 默认（不写 size）；行内操作 / Card extra / 树 sider 头 / 子表操作条 / 表单内联小动作 = `size="small"`；`size="large"` 禁用（实测 0 个）。

**弹窗 footer 立规（031 §016）**：新建/编辑弹窗一律 `<Modal onOk okText="保存" cancelText="取消" confirmLoading>` 内建 footer（JulyUserFormModal 模式），禁手写 footer div + Button。

**JulyAiPrompt 对齐改造（定稿动作）**：DomainFormModal / PromptFormModal 原为「外壳 Modal(footer=null) + 内层 Body 手写 footer div」——与金标准模式偏离。重构为 Body 内嵌 Modal（onOk/confirmLoading 直达保存态），外壳退化为 `open ? <Body key/> : null`（key 重挂载机制保留，回填时序坑不复发）；Button import 随之清理。中途踩坑：第一版把 onOk 挂外壳但 handleSave/saving 在内层 Body 作用域，tsc 报 Cannot find name 抓住后重构修复。

**登记**：031 §011 树主子表金标准 = AI 提示词管理（§021 索引同步 + 修订日志）；菜单管理 julyMenu 降为存量变体，遇改向新蓝本收敛。

**门禁**：tsc 0 / eslint 0 / 检查器 ERROR 0 WARN 1 / dev 200。变更 3 文件（DomainFormModal / PromptFormModal / RenderPreviewModal 删行钮 small）+ 031 四处。


## 5. 014 树主子表开发手册建档 — 下午

**触发**：用户定稿 JulyAiPrompt 为树表金标准后指示「起草文档」。本轮先被「全局弹窗审计 + 拆分」（§4）插入，恢复后完成。

**产出**：`docs/dev-guides/014.tree-master-sub-dev-standard.md`（蓝本 = AI 提示词管理，体例对齐 013）：Before You Start 契约核查（selectTree + 明细五端点决定形态）→ 6 文件拆分与职责边界 → 树壳结构（URL ?domain= 为真源、树/表联动零手写）→ KlsjnhTreeList011 树规格（右键菜单上抛、draggable/onDrop 成对）→ 明细按域直查（pkMt、大字段 getContent 点查、未选域空态）→ Form Modal（Body 内嵌 Modal + okText footer + key 重挂载 + 防环）→ 数据层六行表（types/service/store/mock/URL）→ 8 条自查单 → Worked Example（09-21/22 两日演进）。

**登记**：docs/README.md dev-guides 行内追加 014 链接（对齐内嵌式体例，独立行样式不符合该表格式已改回）。

**门禁**：tsc 0 / eslint 0 / 检查器 ERROR 0 WARN 1 / dev 200。新增 1 文档文件 + README 一处。
