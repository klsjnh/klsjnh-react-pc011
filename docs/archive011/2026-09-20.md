# 2026-09-20 工作日志

## 一、背景

后端换版（11610 → 11160）并完成模块重组，前端同步适配。用户指令三连：

1. 读后端文档 + 代码，摸清重组实况
2. 「低代码去新项目了 你先隐藏」—— 低代码入口隐藏，页面代码保留
3. 「按这个方案开始改造」

后端重组事实（拉取 11160 实时 OpenAPI 核实，详见 `docs/2026-09-20-chat.md`）：

| 变化 | 前端影响 |
|------|----------|
| user / role / userAudit → 独立 iam 模块 | 前缀 `/klsjnh/system011` → `/klsjnh/iam` |
| datasource 只剩 julyDatasource | 前缀 `/klsjnh/datasource` |
| ai011 改名 aicenter | 前缀 `/klsjnh/ai011` → `/klsjnh/aicenter` |
| 新增 julyAiInference / julyAiImage / julyAiAudio | 本期不接入 |
| 新增 messagecenter（33 端点） | 本期不接入 |
| 低代码 / 业务建模后端代码全部移除 | 前端对应请求必然 404 |

## 二、任务清单

- [x] T1 创建 `docs/2026-09-20-chat.md`（011 协议 Step 1）
- [x] T2 删除根目录 `nul` 垃圾文件（Windows 保留设备名，`\\?\` 前缀删除）
- [x] T3 AI011_BASE：`/klsjnh/ai011` → `/klsjnh/aicenter`
- [x] T4 隐藏低代码 + 业务建模菜单入口
- [x] T5 处理 julyBusinessModelingService 的失效引用
- [x] T6 `./script011.sh gate` 自测（本机 bash 解析 node 失败，按脚本同款三步手工执行，结果一致）
- [x] T7 文档同步 + 落 OpenAPI 快照
- [x] T8 汇报，等用户决定是否提交

## 三、执行记录

### T3 aicenter 改名
- `src/services/ai011/julyAiModelProviderService.ts:26` `${AI011_BASE}` 值改为 `/klsjnh/aicenter`，文件头同步注明后端换版
- 动作路径 `/julyAiModelProvider/v1/*` 不变（资源名未变，只换模块前缀）

### T4 菜单入口隐藏
- 数据源有二：`julyMenuStore.useNavMenus` 的接口菜单（生产态）+ `constants.GLOBAL_MENUS`（开发态兜底），两处同批处理
- 路由、页面组件、service/store 全部保留不删

### T5 业务建模 service
- 后端已无 `julyBusinessModeling` 资源，`DATASERVICE011_BASE`（现 `/klsjnh/datasource`）+ action 会拼出必然 404 的 URL

### T6 / T7
见下节执行结果。

## 四、质量自测

| 项 | 命令 | 结果 |
|----|------|------|
| tsc | `node node_modules/typescript/bin/tsc --noEmit` | **EXIT 0**，0 错误 |
| eslint | `node node_modules/eslint/bin/eslint.js .` | **EXIT 0**，0 error / 0 warn（修复 ignores 后） |
| 规范自检 | `node tools/check-klsjnh-react-standards.mjs` | **ERROR 0 / WARN 1**（WARN 为历史遗留 `src/types/view/mock.ts` 的 any，与本次无关） |

> 注：`script011.sh gate` 直接跑会在本机 bash 报「未找到 node 运行时」（脚本内 `command -v node` 与 MSYS 路径 `[ -x "D:/Environment/nodejs/node.exe" ]` 均失败），
> 故按脚本同款三步手工执行，命令与顺序完全一致。
>
> 附带修复：`eslint.config.js` ignores 原为 `*.mjs`（只覆盖根目录），`.workbuddy/`（IDE 工具目录，已 gitignore）与根目录 `.cjs` 一次性脚本被扫出 133 个 node 全局缺失错误导致门禁假红。已补 `.workbuddy` 与 `*.cjs`，`src/` 本身始终 0 错误。

## 五、遗留

- 消息中心 33 端点未接入（用户未明确要求，本期不做）
- `src/pages/backup011/**` 死代码未清理（历史遗留，待授权）
- ~~根目录 `extract-menu.cjs` 仍被 git 跟踪~~ → **2026-09-20 已处理**：与 `temp-extract2.cjs` 一并删除（一次性诊断脚本，读取的 `docs/swagger-live.json` 已于 09-18 清理，无引用）

## 六、收尾（用户批示"继续"后追加）

1. **`script011.sh` node 解析修复**：本机终端实为 **WSL2 bash**（`uname` = Linux），`command -v node` 与 `D:/Environment/nodejs/node.exe` 前缀在其下均失败，导致 gate 报「未找到 node 运行时」。`resolve_node` 补 `/mnt/d/Environment/nodejs/node.exe` 候选（WSL 挂载形态），Git Bash 路径保留。修复后 `./script011.sh gate` 直接可跑。
2. **快照核对**（`docs/contracts/2026-09-20-openapi.json`，157 路径）：模块分布 `iam 25 / system011 45 / storagecenter 24 / aicenter 17 / datasource 8 / messagecenter 33 / demo11 5`，无 lowcode011；iam 下资源确认为 julyUser / julyRole / julyUserAudit；菜单端点确认为 `getUserMenuTree` / `getTree` —— 与前端改动完全吻合。
3. **接口实测**（dev server 11161 → 代理 11160，免密登录取 token 后逐项打）：

| # | 请求 | 结果 | 验证点 |
|---|------|------|--------|
| 1 | `POST /klsjnh/iam/julyUser/v1/loginByUserName` | **200** + token | IAM_BASE 改造正确 |
| 2 | `GET /klsjnh/system011/julyMenu/v1/getTree` | **200**，11 个顶层节点 | 菜单接口更名正确、代理链路通 |
| 3 | `POST /klsjnh/datasource/julyDatasource/v1/selectListByPage` | **200** | datasource 前缀改名正确 |
| 4 | `POST /klsjnh/aicenter/julyAiModelProvider/v1/selectListByPage` | **200** | aicenter 前缀改名正确 |

4. **隐藏必要性实证**：后端菜单表**仍登记** `/dataService011/JulyBusinessModeling`（业务建模）与 `/lowcode011/JulyMetadata`（元数据管理，挂在「数据服务011」目录下）—— 后端代码迁了但菜单数据没清。两条路径均落在 `HIDDEN_NAV_ROUTES` 匹配范围内（root 前缀覆盖 / 子路径精确匹配），前端过滤是必要兜底，不是防御性冗余。
5. `./script011.sh gate` 修复后完整跑通：tsc → eslint → standards，`gate 通过 ✅` EXIT=0。

> 实测环境注意：本机 PowerShell 的 `Invoke-WebRequest` 会被系统代理（Mikrotik）劫持，需 `curl.exe --noproxy *`；body 经 `-d @file` 传入（避免 PowerShell 转义破坏 JSON）。

## 七、AI 中心（aicenter）接入 + 五层目录对齐

**需求**：从老代码（`klsjnh-react-java17-dev011_20260920_013` 的 aiChat011）迁移聊天能力，pages 走 aiCenter 对齐后端，开出「AI 管理 + 聊天（推理）」两个功能。

### 1. 五层 rename：ai011 → aiCenter

`git mv` 迁移 services / stores / types / mock / pages 五层，`AI011_ROUTES` → `AICENTER_ROUTES`，11 个文件引用替换。附带修正：批量替换曾把 `AI011_BASE` 误伤为 `aiCenter_BASE`，已按常量命名风格改回 `AICENTER_BASE`（对齐 `IAM_BASE` / `STORAGECENTER_BASE`）。

### 2. 新增链路（page → service → store 分层，禁 FC / any）

| 层 | 文件 | 要点 |
|----|------|------|
| types | `types/aiCenter/aiInference/vo.ts` | `AiChatRequestVo011` / `AiChatMessageVo011` / `AiChatResponseVo011`，字段逐字对齐快照 |
| service | `services/aiCenter/julyAiInferenceService.ts` | `chat`（信封式）+ `streamChat`（原生 fetch SSE）；供应商复用 `julyAiModelProvider` 管理接口（后端无独立 selectProviders）+ `parseProviderModels` 双兼容解析 |
| store | `stores/aiCenter/aiChatStore.ts` | 消息 / 偏好 / 流式态 + localStorage 持久化（仅欢迎语不落盘） |
| page | `pages/aiCenter/JulyAiChat/index.tsx` | 供应商 + 模型选择、流式气泡（ReactMarkdown）、Enter 发送、清空对话 |
| mock | `mock/aiCenter/aiInference.ts` | mock 逐字流式（不发起网络请求） |
| 路由 | routes.ts + GLOBAL_MENUS | `AICENTER_ROUTES.julyAiChat` + 「AI 聊天」菜单入口 |

### 3. ★ SSE 协议实测发现（重要）

后端 `chat/stream` **不是 OpenAI JSON 包装**，而是**纯文本增量**：

```text
data:Hello\n\ndata: there!\n\n
```

老代码（OpenAI 风格 `startsWith('{')` 过滤）直接搬过来会**丢光所有增量**（已实测复现零输出）。现解析器双协议兼容：`{` 开头走 JSON（choices[0].delta.content），否则纯文本直接 onDelta；且**不 trim 前导空格**（实测后端把词间空格放前导，trim 会拼出 "Hellothere!"）。

### 4. 联调结果（API 模式，真实后端）

- 供应商列表真实返回：`stepfun` / `longcat` / `deepseek` / `asterly`（models 逗号分隔，解析正常）
- `streamChat` 真身实测完整流式：「你好！很高兴与你交流。请问有什么可以帮您的吗？😊」
- ⚠️ 排洞记录：HMR 后浏览器动态 import 命中模块缓存，旧解析器零输出，曾误判「请求未发出」；加 cache-buster 复测证实修复生效
- tsc 0 错误；UI 点击路径（handleSend → streamChat → appendDelta）与 service 真身验证等价，WebView 卡死期间未能补点，逻辑上闭合

## 八、修复：菜单管理「上级菜单」误设必填（轻量变更，未跑门禁）

**现象**：编辑顶级菜单点保存报「请选择上级菜单」；新建菜单也无法提交空上级。

**根因**：`src/pages/system011/julyMenu/index.tsx` 编辑表单与新建弹窗的 `parentId` 均配了 `rules={[{ required: true }]}`，与其余设计相矛盾——

| 证据 | 内容 |
|---|---|
| 后端契约 | `JulyMenuUpdateVo011` / `JulyMenuInsertVo011` 的 required 仅 `[id, menuName, menuType]`（insert 为 `[menuCode, menuName, menuType]`），`parentId` 不要求（docs/contracts/2026-09-20-openapi.json 核实） |
| service 语义 | `moveMenu` 注释「parentId 为空串表示顶级」，`addMenu` 对空 parentId 按顶层挂载 |
| mock 数据 | root 节点 `parentId: ''`（`mock/system011/julyMenu.ts`） |
| UI 自身 | 占位符「（顶级菜单）」+ `allowClear`，与 required 自相矛盾 |

引入版本：ver 0.0.36（`git log -S` 溯源）。

**改动**（同文件 3 处）：
1. 编辑表单 `parentId` 去掉 required，加注释说明留空 = 顶级
2. 新建弹窗 `parentId` 同步去掉 required
3. 菜单 sider Card `extra` 增加「新建顶级菜单」按钮（`openCreate('')`）—— 原先只能从右键「新建子菜单」进入，顶级菜单无创建入口

**顺带修正**：`menuRoute` 原规则「除按钮外必填」导致目录类型（type 1）也必填路由，与 `handleSaveEdit` 注释「目录无路由/组件/权限」及后端契约都不符；root 节点（目录 + `menuRoute: ''`）同样卡保存。编辑/新建两处规则收紧为**仅菜单类型（type 2）必填**。

**验证**：按约定未跑 tsc / 门禁（无明确编译指令）；改动为表单 rules + 一个按钮，逻辑自洽路径已人工核对（openCreate('') → addMenu 空 parentId 走顶层分支）。待用户发令后跑 `./script011.sh gate`。

## 九、AI 模型供应商页改主子表（方案待确认）

**用户判断**：`/aiCenter/julyAiModelProvider`「做的不对，应该是个主子表」。核实后端契约属实：

- 主表 `julyAiModelProvider`（供应商）：providerCode / providerName / baseUrl / models / status / remark
- 子表 `julyAiModelProviderApi`（API 密钥）：apiCode / apiName / apiKey / sortOrder / status / remark，按 **providerCode** 挂接
- 子表端点齐全：`insertApi` / `updateApi` / `logicDeleteApi` / `selectApiListByProvider` / `testConnectionApi`

**现状问题**：主表单与 API 子表全塞在一个 760px 弹窗（ProviderFormModal）里，新建供应商前子表不可见、屏幕利用差、不符合本仓主子表形态（对照 storageCenter / julyPermission）；另主表 `onSaved` 是空实现，保存后列表不刷新。

### 方案：垂直主子表（上主表 / 下子表）

| 区 | 内容 |
|---|---|
| 主表 Card | 供应商列表（列不变），**行点击选中**（复用 `_common.scss` 已备好的 `.master-row-selected` 高亮），行内 测试/编辑/删除 保留 |
| 子表 Card | 标题「API 密钥 · {选中供应商名}」；未选中 → 空态提示；工具条 [新增 API][刷新]；列 apiCode/apiName/apiKey/status/操作(测试/编辑/删除) |
| 高度 | `.page-fill > .table-wrapper` flex:1 天然对半均分，两表各自 `useTableFillHeight` 实测 scroll.y |
| 弹窗 | `ProviderFormModal` 瘦身为纯主表字段；`ApiFormModal` 抽出独立文件挂页面级（原样迁出，props 不变） |
| 选中态 | 页面 state，不进 store 不持久化（刷新回空态可接受；要持久化再效仿 storageExplorerStore） |
| 顺带修 | 主表保存后 `fetchProviderPage({ pageIndex: 1 })` 刷新（现为空实现） |

**不动**：services（selectApiListByProvider / saveProviderApi / removeProviderApi / testProviderApiConnection 均已存在）、mock（14 端点全覆盖）、types、routes、store。

### 任务清单（待确认后执行）

1. 抽出 `pages/aiCenter/JulyAiModelProvider/ApiFormModal.tsx`（从 ProviderFormModal 原样迁出）
2. ProviderFormModal 移除 API 子表区与 ApiFormModal 引用，只留主表字段 + 测试连接
3. index.tsx 改主子表布局：主表行选中联动、子表 Card、双弹窗挂载、onSaved 刷新
4. 样式复用 `.master-row-selected`；子表标题 / 空态文案补齐
5. 验证：mock 模式页面路径人工核对；tsc / eslint / 门禁等用户发令再跑

**已知风险**：① 删供应商时 API 子表后端行为未实测（mock 有级联，后端待验证，不阻塞）；② ApiFormModal 测试连接需已保存的 api id（新建态禁测）保持现状。

### 执行结果（用户批准后落地）

| # | 任务 | 结果 |
|---|------|------|
| 1 | 抽出 `ApiFormModal.tsx` | ✅ 独立文件，props：open / provider / node / onClose / onSaved |
| 2 | ProviderFormModal 瘦身 | ✅ 只留主表 7 字段 + 测试连接；子表区与 ApiFormModal 引用移除 |
| 3 | index.tsx 主子表布局 | ✅ 上主表（行点击选中、`.master-row-selected` 高亮）+ 下子表（标题带供应商名、未选中空态引导、新增/刷新/测试/编辑/删除）；两卡 flex 均分高度各自实测 scroll.y |
| 4 | 选中态派生 | ✅ 按 id 从最新列表派生（编辑后子表标题自动跟新数据、删除自动清选中） |
| 5 | 验证 | ✅ 3 文件 vite transform 200；tsc / eslint / 门禁按约定未跑，待用户发令 |

**改动文件**：`pages/aiCenter/JulyAiModelProvider/{index.tsx, ProviderFormModal.tsx}` 重写 + 新增 `ApiFormModal.tsx`；services / mock / types / routes / store 未动。

## 十、AI 中心新契约 + 聊天页改造 + 两处 bug 修复

### 1. 后端 swagger 变更（live 拉取实测，157 → 168 路径）

- **新资源 `julyAiPrompt`（提示词，5 端点）**：selectListByPage / getByCode / insert（required `[promptCode, promptName, details]`）/ logicDelete / render（`${var}` 替换）。主子表：主表 `JulyAiPromptVo011{id,promptCode,promptName,scene,status}` + 子表 `details[]`（`JulyAiPromptDetailVo011`，required `[domainCode]`，inline/storage 两种内容模式，storage 约定桶 `ai-prompt`）。**无 update 端点**（待与后端确认改法）。
- **`AiChatRequestVo011` 变更**：provider/api 收窄为 code 语义，各新增 `providerId`/`apiId`（二选一）。
- 另：datasource 新增 julySql / julySync（非本期范围）。

### 2. 聊天页（julyAiChat）按用户定稿改造

- 选择器：**KlsjnhSelect031 联动双选**（供应商→密钥，loadChildren 按供应商拉启用密钥）+ 模型 Select 跟随供应商 `models` 字段（逗号分割）；发送门禁三项齐备才放行；请求带 `api`（apiCode）。
- store：`AiChatPref` 增 `apiCode`；**修复偏好供应商失效时 apiCode 残留**（旧密钥会导致门禁放行但键非所属）。
- SSE 解析器加固（后端协议一天内三变）：标准事件解析（同行多 data join `\n`）+ 空 data 事件映射 `\n` + JSON 分支三形态兼容（`choices[0].delta.content` / 扁平 `content` / 纯文本）。
- UI 重做：样式收编 `styles/pages/_aiCenter.scss`（主题令牌，清掉全部硬编码 hex），消息面板卡片化、用户主色气泡右对齐；**清空对话移至发送键上方**（用户指示）。

### 3. 供应商/API 弹窗回填 bug 修复（antd6 时序坑）

- **现象**：编辑供应商/API 时表单全空（只有 initialValues 兜底值）——用户截图确认。
- **根因**：antd6 Modal + destroyOnHidden 下，弹窗内容（Form.Item）挂载**晚于**父组件 effect；effect 里的 `setFieldsValue` 写进字段尚未注册的 store，字段后注册读不到。叠加 Modal 上挂 `key={node?.id}` 触发整体 remount，时序更脆。该 bug 自页面建立起就存在（ver 0.0.48 原码同款写法），非本次主子表改造引入。
- **修法**：表单体拆出 `ProviderFormBody` / `ApiFormBody`，父层按 `node.id` 挂 key → **每次打开都是全新 form 实例 + initialValues 挂载定型**，不依赖 effect 时序；操作按钮随表单体渲染（不再走 Modal footer）。
- 验证（CDP 实测）：编辑供应商 7 字段全回填（状态下拉显「启用」）；编辑 API 全回填；新建默认值正确。

### 4. 端到端验证（API 模式，真后端）

- prompt「给我发个12宫工作的表格」→ 流式完成 → 回复气泡 `table=1 / th=7 / tr=13 / 无裸管道符`，无页面错误。
- 后端 SSE 协议观察记录：半小时内历经「纯文本+空事件换行」→「扁平 JSON」→「OpenAI 风格」三种形态，解析器均已兼容。

## 十一、供应商页全链路 CRUD 复验 + SSE 加固 + 文档梳理（晚间）

### 1. 模型编辑区全链路复验（CDP 实测，用户要求「确认好」）

| 场景 | 结果 |
|---|---|
| 编辑供应商（7 字段回填） | ✅（antd6 时序坑已修，见 §十.3） |
| 改模型列表 → 保存 → 列表刷新 | ✅ 模型数 3→2 |
| 选中行 → API 子表联动 | ✅ |
| 删除 API（Popconfirm） | ✅ 确认按钮文字是「删除」（antd6 惯例，非「确定」） |
| 删除供应商 → 级联清子表 + tab 标签复位 | ✅ |
| 新建供应商 / 新建 API | ✅（上午已验证） |

### 2. SSE 解析器再加固

- **错误信封识别**：后端校验失败时会把 `{"statusCode":4xx,...}` 当作 200 流下发（实测过），此前解析器静默吞掉 → 用户看到空回复。现识别并 `onError(message)` 上抛。
- 连同此前修复，解析器现覆盖：标准 OpenAI 风格（现行）/ 扁平 JSON / 纯文本含空事件换行 / 错误信封，四种形态。

### 3. 文档梳理

- `docs/contracts/2026-09-20-openapi.json`：刷新为 live 168 路径版（+aicenter/julyAiPrompt 5、+datasource/julySql·julySync 6；剔除 servers）
- `docs/contracts/README.md`：当前快照表更新（157→168 同日二次拉取说明）
- `docs/013.api-contract.md`：§013 资源清单补 julyAiPrompt / julySql / julySync（均未接入）；补 SSE 三形态与错误信封警示、AiChatRequestVo011 id/code 二选一说明；修订日志加下午条目
- `docs/015.project-info.md`：**修复自相矛盾**（§011 警示段原写「messagecenter 33 端点未接入」与实际已接入冲突，改为标注未接入的 julyAiPrompt / julySql / julySync）；AI 中心能力描述更新（主子表 + 三级选择聊天）

### 4. 遗留（「其他的」状态）

- **提示词页面（julyAiPrompt）未开工**——用户已批准顺序，排在聊天线之后
- 消息中心后端菜单 update 500（经菜单 API 修 `/messageCenter` → `/messageCenter/index` 时后端 500 空 body，疑后端部署中，待复试验证）
- 今日改动**未过门禁未提交**（等用户发令）

## 十二、API 子表编辑 bug 修复（用户发现「子表编辑也不对」）

**根因**：实测真实后端 `selectApiListByProvider` **不回传 `apiKey` 字段**（响应里直接没有）。编辑弹窗里密钥框带回填空值却仍是**必填** → API 模式下不改密钥就保存不了其他字段（mock 模式的密钥是假的所以没暴露）。

**修法**（`ApiFormModal.tsx`）：
- 编辑态 apiKey 改**选填**（placeholder「留空则不修改」+ extra 说明「后端不回传已保存的密钥」）；新建态仍必填
- 保存时空密钥转 `undefined` 不下发（`apiKey: v.apiKey || undefined`），后端按部分更新保留原密钥（`AiModelProviderApiUpdateVo011` 的 apiKey 非 required）

**验证（CDP）**：编辑态 `required=false` ✓、清空密钥保存成功且原密钥保留 ✓、新建态 5 字段必填 ✓、空表单保存被拦截（3 条字段错误、弹窗不关）✓。
**待办**：API 模式下「部分更新不带 apiKey」的后端语义未实测（VO 契约支持，未在真实数据上验证），用户 API 模式试编辑时留意。

## 十三、晚间一批：聊天页布局 + 供应商弹窗 + 配置状态 + 提示词管理全栈（2026-09-20 晚）

### 1. AI 聊天页底部改左右结构（用户指示）

- 原「清空对话独立行（右上）」+「文本框 + 发送」→ 合并为**左右结构**：左文本框吃满，右列纵向 **清空对话（上）/ 发送（下）**（`.ai-chat-actions` 100px 列、两按钮 flex 等分高度）
- 删 `.ai-chat-clearbar`；样式收编 `_aiCenter.scss`（主题令牌）

### 2. 供应商编辑弹窗（ProviderFormModal）布局 + 去测试连接

- 布局定为：**编码 / 名称 / 排序三列一行**（span 8×3，用户定稿「这样就好了」）；models 整行；状态、备注各自一行
- **主表编辑去掉「测试连接」按钮**（用户指示「主表不该有测试，子表应该有」）：删除 handleModalTest / modalTest 态 / TestFeedbackAlert 渲染 / testProviderConnection 引用；service 的 `testProviderConnection`（`/testConnection`）随之孤立——**未删**，待用户确认后端是否保留该端点后再清
- 子表（ApiFormModal + 行内）测试连接保留

### 3. 配置管理补「状态」字段（用户指示「配置管理少了状态」）

- **契约核实**：后端 `JulyConfigInsertVo011` / `JulyConfigUpdateVo011` **均不含 status**（Java 源码 + 线上 OpenAPI 双重确认），`JulyConfigUseCase.insert/update` 也不收
- **用户裁定「你先做上，我后面改后端」**：前端先行——`JulyConfigUpsertVo`/`UpdateVo` 加 `status?`、`saveConfig` 透传、mock insert/update 支持 status、`ConfigFormModal` 加状态下拉（默认启用、编辑回填）
- saveConfig 内部自带列表刷新，页面 onSaved 空实现无碍

### 4. AI 提示词管理（julyAiPrompt）全栈接入（聊天线之后的既定下一项）

**契约**（线上 11160 实测 11 端点，快照 5 端点已过期）：selectListByPage / getById / getByCode / insert / **update** / logicDelete / insertDetail / updateDetail / logicDeleteDetail / getContent / render。主表 VO 无 details，明细无「按 id 点查」也无「按提示词查列表」端点（use case `details(pkMt)` 已在，差 HTTP 暴露）。

| 层 | 文件 | 要点 |
|----|------|------|
| types | `types/aiCenter/aiPrompt/vo.ts` | 主表 / 明细 / 查询 / insert / update / detailSave / render 七型 |
| service | `services/aiCenter/julyAiPromptService.ts` | 全端点封装；`selectDetailListByPrompt` 按命名惯例（对照 selectApiListByProvider）**约定先行**，待后端补 |
| mock | `mock/aiCenter/julyAiPrompt.ts` | 3 提示词 + 明细 + render 的 `${var}` 替换模拟，全 12 handler |
| store | `stores/aiCenter/julyAiPromptStore.ts` | 列表 / 分页偏好（pageSizePref） |
| page | `pages/aiCenter/JulyAiPrompt/index.tsx` | 主子表：主表行选中（选中态入 URL `?promptId=`）+ 明细 Tabs 子表 + 渲染预览弹窗 |
| page | `PromptFormModal.tsx` | 新建（主表 + Form.List 明细随 insert 一次下发）/ 编辑（仅主表，后端 update 不含明细） |
| page | `PromptDetailEditorPage.tsx` | 明细整页编辑器，路由 `/detail/new` 或 `/detail/:id`（用户要求「子表编辑跳转路由」）；编辑态靠 promptId 拉列表反查明细行 |
| page | `RenderPreviewModal.tsx` | domainCode 选择 + 动态键值变量 + render 结果 pre 展示 |
| 路由/菜单 | routes.ts + constants.ts + mock/julyMenu.ts | `AICENTER_ROUTES.julyAiPrompt(Detail)` + 「提示词管理」菜单 + home 动态路由分支 |

**布局铁律落地**（用户指示「状态和备注必须各自一行写入 ui 铁律」）：`docs/016.coding-standards.md` 新增 **§9.1 表单布局铁律**（短字段各自独占一行 / 长字段整行且正文统一 KlsjnhMarkdown011 / 操作按钮放页头动作区不沉底）。明细编辑器按此重排：页头（返回+标题+保存/取消）→ 编码/模式/排序三列 → 变量声明整行 → **Markdown 编辑器正文** → 状态独立行 → 备注独立行。

**验证**：tsc 0 错误；eslint 0 错误；CDP 实测 mock 全链路（新建带明细→选中联动→跳转编辑→回填→改存→回跳带选中→渲染预览）✓；API 模式主表列表从线上拉取正常（当前后端 prompt 表为空，`total:0`）。

### 5. 遗留

- **后端待补** `selectDetailListByPrompt`（前端已按约定路径先行，mock 已实现；后端加一个 controller 方法暴露现成 use case `details(pkMt)` 即可）——用户称「后端接口应该好了」，但 11160 当晚曾 TCP 不可达（重启中），待复测
- `docs/contracts/2026-09-20-openapi.json` **快照过期**（5 → 实际 11 个 julyAiPrompt 端点），后端恢复后需按 C1 重新落盘
- service 层 `testProviderConnection` / mock `/testConnection` 已成孤立代码，待后端口径明确后删

## 十四、菜单接口全量梳理（2026-09-20 深夜，用户指示「用菜单接口把菜单梳理一下」）

**数据源确认**：`globalConfig.menuFromConfig = false` + `NAV_MENU_SOURCE = 'all'` → 侧边栏权威数据是后端 `july_menu` 表（getTree）。GLOBAL_MENUS 与 mock 树为备用源，同步对齐。

**后端菜单表清理**（经菜单接口 POST update/insert/logicDelete 执行，执行前快照留底可回滚）：

| 类 | 项 | 处理 |
|----|----|------|
| 图标修正（emoji/非法名 → antd 组件名） | 配置管理 ⚙️、审计日志 📝、数据源管理 🗄、数据中心 `Data`、消息中心（空） | → SettingOutlined / FileSearchOutlined / DatabaseOutlined / DatabaseOutlined / NotificationOutlined |
| 图标语义对齐 | Ai中心（铃铛→闪电）、存储管理（面积图→数据库）、文件管理（计算器→文件）、用户管理（用户→团队） | ThunderboltOutlined / DatabaseOutlined / FileTextOutlined / TeamOutlined |
| 路由修正（原路由前端不存在，点了落仪表盘） | 系统监控 /system013/monitor、缓存管理 /system013/cache、审计日志 /system013/auditLogs、服务日志 /system013/serviceLogs | → /business/monitor、/business/cache、/audit、/business/servicelog |
| 删除（模块已迁走/无页面/空壳） | 业务建模、元数据管理、系统日志、测试011、模型011、开发模型016 | logicDelete ×6 |
| 新增 | 提示词管理（/aiCenter/julyAiPrompt，AI 中心下）、消息管理（/messageCenter/index，消息中心下）、数据报表（/reports，应用中心下） | insert ×3 |
| 结构 | 应用中心原嵌在数据中心下（目录套目录） | 提为顶层（parentId=''） |

**踩坑记录（重要）**：后端 `julyMenu/v1/update` 是**全量替换**语义——只传 `{id, menuName, menuType, menuIcon, menuRoute}` 会把 `parentId/sortOrder/permissionCode` 清空，9 个子节点瞬间脱离父级浮到顶层。修复：按清理前快照带**全字段**（parentId/sortOrder/permissionCode/component）重新 update 挂回。前端 service 的 `moveMenu` 之所以传全字段，正是因为这个语义。

**前端同步**：mock 菜单树删业务建模项；GLOBAL_MENUS 删低代码中心 + 业务建模（清理 LOWCODE011_ROUTES import）；HIDDEN_NAV_ROUTES 保留作兜底（后端入口已清，双保险）。

**验证**：getTree 终态 11 顶层分组结构正确；CDP 实测侧边栏 11 分组齐全、图标全部渲染（Ai中心=闪电、数据中心=数据库、消息中心=铃铛）；菜单管理页树形正常。

**追问修正（用户质疑「存储管理对api了吗」）**：网络面板实测三条链路均打 storagecenter 模块——存储管理→`julyStorage/v1/selectListByPage`、点行→`selectBucketListByPage`、文件管理→`julyObject/v1/selectObjectListByPage`，与 system011 无关；页面为主子表（存储实例→存储桶），所见「默认存储/本地磁盘」即后端真实存储配置。另发现两条菜单 **menuCode 错名**（存储管理=`reports`、文件管理=`calc`，早年改菜单留下的）；后端契约 menuCode 更新不可变（`JulyMenuUpdateVo011` 无该字段），按用户批准**删后重建**：`storageInstance`/`storageFile`（路由/图标/排序/父级不变），CDP 复验页面数据与接口正常。

## 十五、提示词页主子表翻转：业务域升主表（2026-09-20 深夜，用户指示「先做业务域再做提示词，主子表反了」）

**背景**：§十三 按后端数据模型建的是「提示词主表 + 业务域明细子表」；用户裁定产品视角应为**先业务域、后提示词**，主子表对调。因后端无独立业务域实体（`domainCode` 仅是明细字段、无域列表接口），采用**前端聚合派生**：域主表由全量提示词的明细去重聚合而成（待后端加域实体后可换真源）。

**改造**（`pages/aiCenter/JulyAiPrompt/index.tsx` 重写）：

| 项 | 原（提示词主） | 现（业务域主） |
|----|----|----|
| 主表 | 提示词列表（分页 + 关键字/场景/状态筛选） | **业务域**（domainCode + 提示词数，聚合派生，行点击选中，选中态入 URL `?domain=`） |
| 子表 | 业务域明细（默认域/财务域…） | **选中域下的提示词全集**（含未配置该域内容的，行内「该域内容」列标记 已配置/未配置） |
| 行操作 | 编辑（提示词主字段） | 编辑内容（跳明细编辑器；未配置则走 `detail/new` 并带 `domain` 预填）/ 设置（提示词主字段弹窗）/ 删除（删提示词级联全域） |
| 渲染预览 | 子表工具栏，需选中提示词 | 工具栏按钮 + 子表行点击选中提示词；打开时预选当前域（`defaultDomain`） |
| 新建提示词 | 弹窗（主字段 + 明细 Form.List） | 不变；选中域时预填第一行明细的域编码（`defaultDomain`） |

**数据链路**：`fetchAllPrompts()`（新增 service，pageSize 200 全量、不碰分页偏好）→ 挂载即拉 → 列表变化触发 N+1 明细拉取（`selectDetailListByPrompt` × 每个提示词，alive 守卫）→ `detailsMap` → 域聚合 memo。N+1 是域主表固有代价（域只能从明细反推，管理端规模可接受）；全部失败才 toast 一次（避免端点未上线时刷屏）。

**顺带补的功能闭环**：老提示词添加新域内容原先无 UI 路径（insertDetail 有端点无入口）——现子表显示全部提示词 + 「未配置」标记 + 新建预填，闭环打通。

**验证（CDP，mock 模式）**：主表聚合正确（default×2 / finance×1）；点域 → URL 带 `?domain=default`、子表 3 条提示词 + 该域内容标记（2 已配置 / 1 未配置）；已配置→跳 `detail/dtl-0001?promptId=prm-0001` 回填完整；未配置→跳 `detail/new?promptId=prm-0003&domain=default` 且**域编码预填 default**；tsc / eslint / 规范门禁全绿。

**再版（用户看了实图后定稿「模仿角色页：左边业务域、右边提示词」）**：上下主副表再次推倒，改**左右分栏**——完全复刻权限管理（`permission-layout/sider/main` + List 选中态 + `.domain-item.active` 高亮，样式对齐 `role-item`）：左 Card「业务域」（聚合域列表 + 刷新），右 Card「提示词 · {domain}」（提示词表 + extra 新建提示词/渲染预览）；域选中仍走 URL `?domain=`，提示词行点击选中供渲染预览。数据链路与操作（编辑内容/设置/删除/该域内容标记）原样保留。CDP 截图复核：左侧 default（2 个提示词，选中高亮）+ finance，右侧 3 条提示词行列操作齐全；API 模式下域表为空并有「后端端点待上线」提示（符合预期）。门禁全绿。

**CRUD 补齐（用户追问「新增编辑删除呢」→「删除不要在这里」）**：域内容（detail）的删除原先随页面翻转丢失，曾误加到明细编辑器页头，用户裁定**编辑器页头只留 返回列表/保存/取消**，删除放列表页行内。最终形态——右侧提示词表行操作四件套：**编辑内容**（已配置→编辑器；未配置→新建并预填当前域）/ **设置**（提示词主字段弹窗）/ **删除内容**（仅已配置行出现，删该域下这条 detail，主表不受影响）/ **删除提示词**（删主表级联全部域内容）。另修两个回归：① 编辑器 `backPath` 仍是旧设计 `?promptId=`，保存/取消/删除回跳后丢域选中——改为优先带 `?domain=`；② `detailEditorPath` 对已配置明细不带 domain——统一带上。CDP 实测「删除内容」：toast 成功、该行 该域内容 已配置→未配置、删除内容按钮随之消失、其余行不受影响。门禁全绿。

**页头再调（用户：「返回列表旁边是保存，没有取消，保存和取消不在右边」）**：明细编辑器页头改为**返回列表 + 保存 同在左侧**（取消去掉——返回即放弃），右侧留空。CDP 快照确认 headerBtns = [返回列表, 保存]。

**后端接口复查（用户称「后端接口都好了」，实测未到位）**：178 路径全量核对——11160 上 `selectDetailListByPrompt` 仍 404、无业务域实体端点；本地后端源码 `AiPromptController` 仍是 11 端点（use case 的 `details(pkMt)` 早在，差 HTTP 暴露）。需用户在 controller 补一个方法（照 `selectApiListByProvider` 的形态）并重启：
```java
@PostMapping("/selectDetailListByPrompt")
public Response011<List<JulyAiPromptDetailVo011>> selectDetailListByPrompt(@RequestBody JulyAiPromptDetailQueryVo011 vo) {
    return Response011.success("ai prompt detail list", promptUseCase.details(vo.getPromptId()).stream().map(this::toDetailVo).toList());
}
```
（`toDetailVo` 为 detail→VO 映射；Query VO 收 promptId。）上线后前端零改动即生效（service 已按该路径约定先行）。

## 十六、业务域实体 CRUD（前端先行）+ 明细编辑器页头再调（2026-09-20）

**背景**：用户要求「业务域左上角有 CRUD」并让核对后端/swagger——全量 178 路径核实：域实体端点不存在、`selectDetailListByPrompt` 仍未上线。按既定「前端先行」套路建设。

**域实体五层**（后端按此补 controller 即可零改动对接）：

| 层 | 文件 | 要点 |
|----|------|------|
| types | `types/aiCenter/aiDomain/vo.ts` | `JulyAiDomainItem{id,domainCode,domainName,sortOrder,status,remark}` + Query/Insert/Update + `JulyAiDomainDetailRef{detailId,promptId}`（级联引用） |
| service | `services/aiCenter/julyAiDomainService.ts` | 约定端点 `julyAiDomain/v1/{selectListByPage,insert,update,logicDelete}`；`removeDomain` 级联 logicDeleteDetail、`renameDomain` 级联 updateDetail 同步明细 domainCode |
| mock | `mock/aiCenter/julyAiDomain.ts` | default/finance/marketing 三域 + 全 CRUD handler（domainCode 查重） |
| store | `stores/aiCenter/julyAiDomainStore.ts` | 列表态 + 分页偏好 |
| page | `DomainFormModal.tsx` + index.tsx 左上角 | 域弹窗（编码/名称/排序/状态/备注）；左 Card 标题区（左上角）CRUD：新建/编辑/删除/刷新，编辑删除作用于当前选中域 |

**页面联动**：域列表换实体源（+ 明细聚合计数）；改名后 URL 同步新码并重拉提示词明细（计数/标记刷新）；删域清 URL 选中。修两个自家 bug：① 改名后计数不刷新（rename 后漏拉提示词）；② URL 同步比较的是刷新后的新值（先存旧码再比）。

**明细编辑器页头**（用户：「返回列表旁边是保存，没有取消，保存和取消不在右边」）：改为**返回列表 + 保存 同在左侧**，去掉取消，右侧留空。

**验证（CDP，mock 模式）**：新建域（hr 入列）✓；改名 default→default2（明细级联、计数恢复 2、URL 同步、右栏标题跟随）✓；删域 finance（toast 成功、域消失、URL 清空、该域明细级联清掉）✓；门禁全绿。
