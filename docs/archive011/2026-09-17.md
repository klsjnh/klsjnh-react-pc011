# 2026-09-17 工作日志

## 今日任务

1. 存储中心（storagecenter）主子表收尾：存储实例主表 → 选中后桶子表
2. **项目审计**（规范门禁 / 架构红线 / 工程卫生 / 死代码）并按审计结论整改

---

## 一、审计结论

**可用，无致命运行时缺陷**；但项目自设门禁跑不过，且存在「自己定的红线自己违反」。

| 类别 | 结论 |
|------|------|
| ① 门禁 | `tools/check-klsjnh-react-standards.mjs` 跑出 **ERROR 25 / WARN 8**，按 `017 §D2` 门禁失败不允许提交 |
| ② 架构红线 | `017 §A2` 禁止「HashRouter + `/*` 通配 + 组件内 switch 分发」，现状恰是此形态（`main.tsx` `<HashRouter>` → `App.tsx` `path="/*"` → `home/index.tsx` `PAGE_MAP` 分发）——**待裁决** |
| ③ 工程卫生 | 契约快照散落 4 份（1 份 0 字节）；npm/pnpm 双锁文件；根目录 9 个临时文件；文档断链；`eslint.config.js` 注释仍写 pnpm |
| ④ 死代码 | `pages/backup011/**`（内含坏 import）、`julyStorage/StorageInstancePage.tsx`、`julyStorage/StorageInstancesModal.tsx`、`stores/storage011/storageExplorerStore.ts`、`styles/global.css` |

**做得好的（未动）**：`api/request.ts`（401 收口 + 凭据白名单 + 节流 + 超时 + 信封解包）、`config/theme.ts`（SEEDS 单一真源驱动 10 套换肤）、`useTableFillHeight`（实测高度 + 有 cleanup）、依赖零冗余、无页面绕过 service 直连 api。

---

## 二、本次整改（已完成）

### 1. 规范门禁 ERROR 25 → 0

- **相对路径导入** 20 文件 39 处 → 统一 `@/` 别名（分发于 components/system011、lowcode011、storageCenter、services、types、backup011）
- **`React.FC`** 4 处 → 普通函数 + 显式 props 类型（`IconPicker` / `KlsjnhSql011` / `KlsjnhMarkdown011` / `ProviderFormModal`），并清掉随之失效的默认 `React` 导入
- **`console.log`** 1 处 → `console.info`。⚠️ 该处在 `business-pages.tsx` 的 **Markdown 演示代码块字符串内**，属门禁正则误报，非真实运行时输出

### 2. WARN（`any`）8 条 → 3 条

| 处理 | 说明 |
|------|------|
| `(e as any)?.errorFields` 3 处 | → `typeof e === 'object' && 'errorFields' in e`（去掉 any 且更准确） |
| `catch (e: any)` 2 处 | → `catch (e)` |
| `ColumnsType<any>` + 18 处 render `any` | → `ColumnsType<{ key: number; name: number }>` + `unknown` |
| `MockEnvelope<any>` | → `MockEnvelope<unknown>`（request.ts 处 `env.data as T` 不受影响） |
| `getMockResponse(action, body?: any)` | → `body?: object` |
| **保留** | `MockHandler` 的 `body: any`（按 action 字符串分发、请求体异构，handler 内自行收窄；mock handler 共 91 处 `body.x` 取值，强类型化收益低风险高）与 `mock/lowcode011` 的 `cleanSub(rows?: any[])` — 已就地加注释说明 |

### 3. antd 6 命名收口

`destroyOnClose` → `destroyOnHidden`，12 文件 14 处（含注释）。依据：antd 6.6.4 `modal/interface.d.ts` 中 `destroyOnClose` 已标 `@deprecated Please use destroyOnHidden instead`。

### 4. 文档断链

`019.backend-api-review.md`（不存在）→ **`019.lowcode011-findings.md`**，覆盖 3 处引用：`Agent.md` 目录树、`Agent.md` 阅读顺序表（并补上缺失的 `docs/` 前缀）、`docs/011.agreements.md` 台账；同时把描述文字由「后端接口质量评审」改为与该文件实际内容一致的「低代码模块现状」。

### 5. 工具链

- `package.json` 新增 `typecheck`（`tsc --noEmit`）/ `lint`（`eslint .`）/ `standards` 三个脚本；`build` 仍只做 `vite build`
- `eslint.config.js`、`script011.sh` 里的 `pnpm add -D` 提示 → `npm install -D`
- 说明：真正的门禁入口是 `./script011.sh gate`（tsc → eslint → 规范自检），脚本已齐备

---

## 三、未处理（**等授权**，涉及删/移文件）

1. 契约快照散落 4 份：`docs/swagger-api-docs.json`、`docs/swagger-api-docs-new.json`（**0 字节**）、`docs/swagger-live.json`、根 `storagecenter-openapi-full.json`（约 1MB）——违反 `017 §C1`（应落 `docs/contracts/{YYYY-MM-DD}-openapi.json`）
   - ✅ **2026-09-18 已整改**：三份快照实为**同一服务**的不同时点（`swagger-api-docs.json` 50 条 / `swagger-live.json` 72 条 / `storagecenter-openapi-full.json` 137 条）。已合并为一份 `docs/contracts/2026-09-17-openapi.json`（以最全的 137 条为准，`lowcode011/julyMetadata` 恰为 7 条，与本节「7 → 11」吻合，故定 2026-09-17）；**剔除 `servers` 里的内网地址**；删除其余快照。`src/types/system011/index.ts` 的来源注释同步更新。
2. 根目录临时文件 9 个：`dev81.log`、`all-paths.txt`、`extract-all.ps1`、`extract-menu.cjs`、`extract-menu.mjs`、`temp-*.mjs`、`tmp_*.mjs`（**均已被 git 跟踪**）
   - ✅ **2026-09-18 已整改**：`all-paths.txt`、`extract-all.ps1`、`extract-menu.cjs|mjs`、`temp-*.{mjs,cjs}`、`tmp_*.mjs`、`src/stores/createStore.ts.orig` 全部删除（`dev81.log` 已不在）；`.gitignore` 补 `tmp_*` / `temp-*` / `*.orig`
3. 双包管理器共存：`package-lock.json` + `pnpm-lock.yaml` + `pnpm-workspace.yaml`（本机实际用 npm）
   - ✅ **2026-09-18 已整改（用户裁定：统一为 pnpm）**：`.gitignore` 解除对 `pnpm-lock.yaml` 的忽略并生成入库；删除 `package-lock.json`（保持忽略）；`pnpm-workspace.yaml` 修正为 `onlyBuiltDependencies: [esbuild]`；`package.json` 增 `packageManager: pnpm@10.18.3`
4. 死代码 5 处（零引用，已逐一核实）
5. `017 §A2` 架构红线：改实现，还是把红线注明现状
6. `docs/2026-09-12.md` 未归档至 `docs/archive011/`（违反 `017 §E4`）

---

## 四、待验证

按约定**未执行** `tsc` / `eslint`（无明确编译指令不跑）。下一步建议：

```bash
./script011.sh gate      # tsc → eslint → 规范自检
```

规范自检一项已单独复跑：**ERROR 0 / WARN 3**。

---

## 五、低代码链路贯通（17:1x）

用户裁定「**1 bug 修了、2 逻辑照搬、3 写文档；接口有的对上，没有的说明是 mock**」后落地。

### 5.1 修 · 契约错配（5 处，全部是前端调错）

| # | 位置 | 修正前 | 修正后 |
|---|------|--------|--------|
| 1 | `getModelingById` | `api.post` 打 GET 端点 | `api.get(..., { id })` |
| 2 | `getModelData` | `api.post` + 传 `modelCode` | `api.get(..., { id })` |
| 3 | `probe` 消费形状 | 期望 `columns: [{name,type}]` | 消费 `fieldData`（后端 `probeAndInfer` 会**推断字段**） |
| 4 | 输出嵌套层级 | `fieldData` 当顶层读 | `fieldData` 嵌在 `metaData` 下 |
| 5 | 字段子项字段名 | `fieldCode/columnName/dataType/...` | `code/name/fieldType/length/notNull/defaultValue` |

> 本项目的方法错配表现为 **HTTP 500**（`HttpRequestMethodNotSupportedException`），**不是 405** —— 排查时优先核对方法而非找后端。

### 5.2 照搬 · 后端缺的能力走占位

- **设计器**：6 Tab（字段 / 显示列 / 服务 / 数据初始化同步 / 开放 API / JSON）+ 顶部动作条（保存并返回 / 发布建表 🧪 / 发布菜单 🧪 / 运行时页）；新建态（无 `objectName`）时 PENDING 动作全禁用。
- **运行时页**：按元数据动态渲染列表 / 表单 / 查询区，服务开关控制增删改按钮显隐。
- **三页按 `objectName` 串起**：业务建模 ↔ 元数据 ↔ 运行时。

### 5.3 文档

- 新建 **[020.lowcode011-interface-matrix.md](020.lowcode011-interface-matrix.md)** —— 三档分流（A 真实接口 18 action / B 契约错配 5 处 / C PENDING-BACKEND 13 个占位函数）+ 实时 OpenAPI 证据 + 后端补齐后的切换方式。
- 同步索引：[README.md](README.md) 阅读入口表、[011.agreements.md](011.agreements.md) §015 目录语义表。

### 5.4 约定偏差修正

- 业务建模列表页 `#9ca3af` 硬编码 → `var(--text-muted)`；工具栏按钮 `type="primary"` → `color="primary" variant="filled"`。

### 5.5 验证

12 个低代码相关文件 Vite transform **全 200**；门禁红线正则（`React.FC` / `any` / `@ts-*` / `console.log` / 硬编码 hex）**全零命中**。
按约定**未跑** `tsc` / `build` / 门禁脚本（等明确指令）。

---

## 六、后端 039 一期上线 → 前端切换真实接口（17:3x）

**触发**：用户「你看看后端 做了一些东西」。

### 6.1 后端交付了什么（源码 + 实时 OpenAPI + 实测三重核实）

后端启动 **039「低代码设计器与运行时」**，定案「全链路做，分三期」：
`java17/docs/requirement011/039.topic-lowcode-designer-runtime.md`（业务）+ `requirement013/039...`（技术方案）。

| 期 | 主题 | 端点 | 状态 |
|----|------|------|------|
| 一 | 设计闭环（**零改库**） | `listModels` `load` `save` `previewDdl`（+ 规划中的 `probeSql` `buildFromProbe` `saveDraft` `publishMeta`） | ✅ **已上线** |
| 二 | 发布闭环（写库） | `publish` `syncData` `importDataFromSql` `importStatus` | ⏳ |
| 三 | 运行时 + 开放 API | `publishMenu` `listRuntimeMenus` `initialize` + 动态 CRUD + `openApiConfig` `saveOpenApiConfig` `rotateApiKey` | ⏳ |

数据模型增量（`docs/sql/july_metadata_publish.sql`，一期一次到位、已在 dev 执行）：
`july_metadata` 加 9 列（`publish_status` / `version` / `physical_table` / `backup_table` / `probe_sql` / `sql_code` / `data_source_code` / `data_initialized` / `last_sync_at`）+ 新表 `july_metadata_version`（发布快照）+ `july_metadata_open_api`（开放 API 配置）。

**实测**（`total 137 → 141`，`lowcode011 7 → 11 条`）：`listModels` / `load` / `previewDdl` 均 200；`previewDdl` 回 `CREATE TABLE IF NOT EXISTS \`lc_bpm_user011\` (...)`。

> ⚠️ 后端这批代码**尚未提交**（`git status` 全 `??`，HEAD 仍 `ver 0.0.22`）。

### 6.2 前端切换（本次改动）

| # | 改动 | 文件 |
|---|------|------|
| 1 | **`previewDdl` 切真实接口**（`GET /v1/previewDdl?objectName=`，解包 `{ddl}`）；**删除**本地 DDL 生成器（`buildDdl` / `pendingPreviewDdl`）—— 本地实现缺 `lc_` 前缀、`IF NOT EXISTS`、无 id 时补主键等规则，与后端不一致会误导 | `services/lowcode011/metadataDesignerService.ts`、`mock/lowcode011/pendingBackend.ts` |
| 2 | 新增设计器 service 四件套 `listMetadataModels` / `loadMetadataDto` / `saveMetadataDto` / `previewMetadataDdl` | `services/lowcode011/julyMetadataService.ts` |
| 3 | 新增 action 常量 4 个（含 `designerSave`，注释区分两组口径与 GET/POST） | `services/lowcode011/actions.ts` |
| 4 | 新增 MetaDTO 契约类型（`JulyMetadataMetaDto011` / `...MetaHeader011` / `...MetaField011` / `...MetaDisplay011` / `...MetaService011` / `JulyMetadataModelRow011`） | `types/lowcode011/julyMetadata/vo.ts` |
| 5 | mock 补齐 4 个 handler：`listModels` / `load` / `save` / `previewDdl`；其中 **`previewDdl` 逐字照抄后端 `MySqlMetadataDdlGenerator`**（含类型映射 / 标识符白名单 / 无 id 补主键）；新增 MetaDTO 双向转换 | `mock/lowcode011/julyMetadata.ts` |
| 6 | 运行时页**无 `objectName` 时**用真实 `listModels` 列可选对象（可点选 / 行点击进入），原「缺少参数」警告页改为模型选择列表 | `pages/lowcode011/JulyMetadata/SchemaRuntimePage.tsx` |
| 7 | `pendingPublish` 版本号改 `0.0.1` 递增**字符串**（对齐后端 §021 风险条）；物化表名统一 `lc_` 前缀 | `mock/lowcode011/pendingBackend.ts` |
| 8 | 过时文案收敛：`docs 038/033` 结论 → 后端 **039 分期**；`type="primary"` → `color="primary" variant="filled"`（2 处） | 设计器页 / 运行时 service / 类型文件 |

**未动**（有意保守）：设计器页仍走 038「一主三子」口径的 `getById` / `insert` / `update`（已验收跑通），MetaDTO 的 `load` / `save` 作为**新增能力**接入，不做口径替换。

### 6.3 新发现的两个后端坑（已写进 020 §020.9）

1. **`publishStatus` / `version` 是硬编码假数据** —— `listModels` 恒回 `'draft'`、`load` 恒回 `''`（`JulyMetadataDesignerUseCase` 源码写死），发布态列虽已建但未读取。**前端不得据此判断发布状态**。
2. **`GET /load` 缺参回 500 而非 400** —— `MissingServletRequestParameterException` 未被 `GlobalExceptionHandler` 映射，同类 GET 端点（`previewDdl` / `getById` / `getByObjectName`）同理。

### 6.4 验证

11 个低代码文件 Vite transform **全 200**；红线正则 `React.FC` / `any` / `@ts-*` / `console.log` / 硬编码 hex / 相对导入 **零命中**（余 1 处 `type="primary"` 在 Modal `footer` 确认按钮，属 antd 惯例、非工具栏，保留）。
按约定**未跑** `tsc` / `build` / 门禁脚本。

### 6.5 文档同步

`020.lowcode011-interface-matrix.md` 全量刷新：A 档 18 → **22**（+ 设计器 4）、C 档 13 → **12**（`previewDdl` 移出）、新增 §020.2.2 MetaDTO 契约与 DDL 生成规则、§020.6 实测更新为 141 / 11 条、§020.9 风险补 3 条（假发布态 / 500 缺参 / 后端未提交）。

---

## 七、后端 039 二期上线 → 发布 / 数据同步切真实接口（18:0x）

**触发**：用户「后端有重大变化」。

### 7.1 后端二期交付（源码 + 实时 OpenAPI + 端到端实测三重核实）

`total 141 → **144**`，`lowcode011 11 → **14** 条`，新增 **`publish` / `importDataFromSql` / `importStatus`**。

| 期 | 端点 | 状态 |
|----|------|------|
| 二 | `publish`（POST）`importDataFromSql`（POST）`importStatus`（GET） | ✅ **已上线** |
| 三 | `publishMenu` `listRuntimeMenus` + 动态 CRUD + `openApiConfig` `saveOpenApiConfig` `rotateApiKey` | ⏳ 仍未实装 |

后端新增落点：`JulyMetadataPublishUseCase` / `JulyMetadataDataSyncUseCase` / 两个 Controller
+ domain `JulyMetadataVersion(Repository)` / `MetadataDdlGeneratorPort` / `MetadataDdlExecutorPort` / `MetadataDataWriterPort`
+ infra `MySqlMetadataDdlGenerator` / `DatasourceDdlExecutor` / `JdbcTemplateMetadataDataWriter` / `JulyMetadataVersion(Po|Mapper|RepositoryImpl)`
+ 配置 **`krt.lowcode.ddl-execute.enabled: true`**（`DatasourceDdlExecutor` 的 DDL 执行开关，默认 false）。

### 7.2 实测（只读 + 错误路径，未触发写库）

```
GET  /importStatus?objectName=bpm_user011 → 200 {dataInitialized:true, physicalTable:"lc_bpm_user011", publishStatus:"published"}
     ★ 该对象此前**已被真实发布过**（物理表已建、数据已初始化）
GET  /importStatus?objectName=__nope__    → 200 {dataInitialized:false, physicalTable:null, publishStatus:"draft"}   ← 不存在也回 200
GET  /importStatus（缺参）                 → 500（MissingServletRequestParameterException 未映射）
POST /publish {}                          → 400 objectName required
POST /importDataFromSql {objectName:"__nope__"}    → 404 record not found, id=__nope__
POST /importDataFromSql {objectName:"bpm_user011"} → 400 dataSourceCode required
```

### 7.3 前端切换（本次改动）

| # | 改动 | 文件 |
|---|------|------|
| 1 | 新增二期 action 常量 3 个（`publish` / `importDataFromSql` / `importStatus`），头注释补 GET/POST 归属 | `services/lowcode011/actions.ts` |
| 2 | 新增真实接口三件套：`publishMetadata` / `getMetadataImportStatus` / `importMetadataDataFromSql` | `services/lowcode011/julyMetadataService.ts` |
| 3 | **`publish` / `importStatus` / `importDataFromSql` 切真实接口**；分流表按「一 / 二期 ✅、三期 ⏳」重排 | `services/lowcode011/metadataDesignerService.ts` |
| 4 | **删除二期占位**（`pendingPublish` / `pendingImportStatus` / `pendingImportDataFromSql` + `DesignerState` / `nextVersion` / `TABLE_PREFIX`）—— 避免「真接口 + 假实现」并存误导 | `mock/lowcode011/pendingBackend.ts` |
| 5 | 类型重写：`PublishResult` 加 `ddl`、`ImportDataPayload` 加必填 `dataSourceCode`、`ImportStatusResult` 去掉后端不返的 `lastSyncAt` 强依赖、`ImportDataResult` 标注三值恒 0 | `types/lowcode011/metadataDesigner.ts` |
| 6 | mock 补二期 3 个 handler + 发布态内存 `Map` + `generateAddColumns`（**逐字照抄后端 `MySqlMetadataDdlGenerator#generateAddColumns`**：只 ADD 不 DROP、版本号 `0.0.1` 递增串、未发布 → 400、对象不存在 → 404） | `mock/lowcode011/julyMetadata.ts` |
| 7 | 数据同步 Tab 补 **`dataSourceCode` + 源 SQL** 两个必填输入（后端缺任一 → 400），未发布时拦截并提示；「发布建表」按钮去掉 🧪、加 loading | `pages/lowcode011/JulyMetadata/MetadataFormPage.tsx` |
| 8 | 运行时页新增**真实发布态**展示（`importStatus`）：已发布 / 待发布 + 物理表名 + 数据已初始化；未发布时给「去设计器」引导 | `pages/lowcode011/JulyMetadata/SchemaRuntimePage.tsx` |
| 9 | 发布弹窗改红色警示（**真正执行 DDL**，`okButtonProps={{danger:true}}`）+ 明示 `migrateData` 为预留参数 + 展示「上次发布实际执行的 DDL」 | `pages/lowcode011/JulyMetadata/MetadataFormPage.tsx` |

### 7.4 新发现的四个后端坑（已写进 020 §020.9）

1. **`migrateData` / `includeDeleted` 是预留参数** —— 源码注释 `reserved for the data migration step`，**收而不用**：不会 rename 旧表、不迁移数据，`backupTable` 恒 `null`。
2. **`importDataFromSql` 的 `sqlCode` 实为 SQL 文本** —— 后端直接喂给 `BusinessModelingExecuteCommand.sqlContent`。命名误导，UI 按「源 SQL」呈现。
3. **`updated` / `unchanged` / `skipped` 恒为 0** —— `JulyMetadataDataSyncUseCase` 源码写死，只有 `inserted`（=本页处理行数）可信。前端不再宣称「更新 / 未变」。
4. **`listModels` / `load` 的 `publishStatus` 二期后仍未修** —— 依旧硬编码 `draft` / `''`。**唯一可信发布态 = `importStatus`**，设计器页与运行时页均已改用它。

### 7.5 验证

11 个文件 Vite transform **全 200**；红线正则（`React.FC` / `any` / `@ts-*` / `console.log` / 硬编码 hex）**零命中**；二期占位残留引用检索 **0 命中**。
按约定**未跑** `tsc` / `build` / 门禁脚本。

### 7.6 文档同步

`020.lowcode011-interface-matrix.md` 全量重写：A 档 22 → **25**（+ 二期 3）、C 档 12 → **9**（设计器 8 → 5）；新增 §020.2.3 二期契约与两段服务端行为说明、§020.6 实测更新为 **144 / 14 条** + 二期端点样本 + 后端新增落点表、§020.9 风险 9 → **13** 条。
